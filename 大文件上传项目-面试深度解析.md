# 大文件上传项目 - 面试深度解析（字节/京东/百度级别）

> 基于简历亮点：大文件分片上传、断点续传、Web Worker 多线程计算、并发控制、重试机制

---

## 目录

1. [项目背景与方案设计](#一项目背景与方案设计)
2. [核心技术架构](#二核心技术架构)
3. [深度技术问题](#三深度技术问题)
4. [性能优化与工程实践](#四性能优化与工程实践)
5. [系统设计与扩展](#五系统设计与扩展)
6. [代码实现细节](#六代码实现细节)
7. [面试表达技巧](#七面试表达技巧)

---

## 一、项目背景与方案设计

### 1.1 业务背景（必问）

**Q: 为什么要做大文件上传优化？遇到了什么具体问题？**

**标准回答：**
在实际业务场景中（如视频上传、文档管理系统），用户上传大文件时遇到以下痛点：

1. **传输不稳定**：网络波动导致上传失败，需要从头开始（用户体验差）
2. **性能瓶颈**：
   - 大文件（如 1GB+）一次性上传占用大量内存，浏览器可能崩溃
   - 主线程计算 MD5 阻塞 UI，页面卡顿
3. **带宽浪费**：相同文件重复上传，占用服务器存储和带宽
4. **用户体验差**：无法感知进度、无法暂停/恢复、失败后无法续传

**数据支撑（加分项）：**

- 优化前：500MB 文件上传失败率 35%，平均耗时 8 分钟
- 优化后：失败率降至 5%，平均耗时 3 分钟（断点续传 + 并发优化）

---

### 1.2 方案选型（深挖）

**Q: 为什么选择分片上传而不是其他方案（如流式上传、WebSocket）？**

**对比分析：**

| 方案         | 优点                             | 缺点                       | 适用场景         |
| ------------ | -------------------------------- | -------------------------- | ---------------- |
| **分片上传** | 支持断点续传、并发上传、失败重试 | 需要服务端支持合并         | 大文件（>10MB）  |
| 流式上传     | 实现简单、内存占用小             | 不支持断点续传、失败需重传 | 小文件、实时数据 |
| WebSocket    | 双向通信、实时反馈               | 服务端复杂、不支持断点续传 | 实时聊天、推送   |
| Base64       | 无需额外处理                     | 体积增大 33%、性能差       | 极小文件（<1MB） |

**选择分片上传的原因：**

1. 支持断点续传（核心需求）
2. 可并发上传，充分利用带宽
3. 单个分片失败只需重传该分片
4. 业界成熟方案（阿里云 OSS、七牛云、腾讯云 COS 均采用）

---

### 1.3 分片大小选择（高频考点）

**Q: 为什么选择 5MB 作为分片大小？如何动态调整？**

**理论分析：**

```
分片过小（如 1MB）：
- 分片数量多 → HTTP 请求开销大（TCP 握手、TLS 握手）
- 服务端合并耗时长（IO 操作多）
- 并发控制复杂度高

分片过大（如 50MB）：
- 单个分片失败重传成本高
- 内存占用大（浏览器需要读取整个分片到内存）
- 进度更新不够细腻
```

**最佳实践：**

- **5-10MB**：业界标准（阿里云 OSS 推荐 5MB，AWS S3 推荐 5-100MB）
- **动态调整策略**（进阶方案）：

  ```javascript
  // 根据网络速度动态调整
  function calculateChunkSize(networkSpeed) {
    if (networkSpeed > 10 * 1024 * 1024) return 10 * 1024 * 1024 // 10MB
    if (networkSpeed > 5 * 1024 * 1024) return 5 * 1024 * 1024 // 5MB
    return 2 * 1024 * 1024 // 2MB
  }

  // 根据文件大小动态调整
  function adaptiveChunkSize(fileSize) {
    if (fileSize < 50 * 1024 * 1024) return 2 * 1024 * 1024 // <50MB: 2MB
    if (fileSize < 500 * 1024 * 1024) return 5 * 1024 * 1024 // <500MB: 5MB
    return 10 * 1024 * 1024 // >500MB: 10MB
  }
  ```

**实际项目中的权衡：**

- 固定 5MB：实现简单，适合大多数场景
- 动态调整：需要测速逻辑，增加复杂度，但用户体验更好

---

## 二、核心技术架构

### 2.1 整体架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                         前端架构                              │
├─────────────────────────────────────────────────────────────┤
│  UI 层 (Vue Component)                                       │
│    ├─ FileUpload.vue  (文件选择、拖拽上传)                   │
│    └─ FileList.vue    (上传列表、进度展示、操作按钮)          │
├─────────────────────────────────────────────────────────────┤
│  逻辑层 (Composable)                                         │
│    └─ useUpload.ts    (响应式状态管理、事件监听)              │
├─────────────────────────────────────────────────────────────┤
│  服务层 (Service)                                            │
│    └─ UploadService   (上传核心逻辑、状态机管理)              │
│         ├─ 文件队列管理                                       │
│         ├─ MD5 计算调度                                       │
│         ├─ 分片上传控制                                       │
│         └─ 断点续传逻辑                                       │
├─────────────────────────────────────────────────────────────┤
│  工具层 (Utils)                                              │
│    ├─ WorkerPool      (线程池管理)                           │
│    ├─ md5.worker.ts   (MD5 计算 Worker)                      │
│    └─ upload.ts       (HTTP 请求封装)                        │
└─────────────────────────────────────────────────────────────┘
                              ↓ HTTP
┌─────────────────────────────────────────────────────────────┐
│                         后端架构                              │
├─────────────────────────────────────────────────────────────┤
│  API 层                                                      │
│    ├─ POST /minio/init       (初始化上传)                    │
│    ├─ GET  /minio/check/:md5 (检查秒传)                      │
│    ├─ PUT  /minio/chunk      (上传分片 - 预签名 URL)          │
│    └─ POST /minio/merge/:md5 (合并分片)                      │
├─────────────────────────────────────────────────────────────┤
│  存储层                                                      │
│    └─ MinIO / OSS / S3       (对象存储)                      │
└─────────────────────────────────────────────────────────────┘
```

---

### 2.2 核心流程详解

```
用户选择文件
    ↓
[1] 添加到上传队列
    - 生成 UUID (文件唯一标识)
    - 创建分片信息 (ChunkInfo[])
    - 初始化状态 (PENDING)
    ↓
[2] 计算文件 MD5 (Worker Pool)
    - 提交任务到线程池
    - Worker 分片读取文件 (2MB/片)
    - 增量计算 MD5 (SparkMD5)
    - 实时上报进度 (0-100%)
    ↓
[3] 检查秒传 (GET /check/:md5)
    - 服务端查询 MD5 是否存在
    - 存在 → 直接返回 URL (秒传成功)
    - 不存在 → 继续下一步
    ↓
[4] 初始化上传 (POST /init)
    - 请求参数: {md5, chunkSize, chunkNum, ...}
    - 响应数据: {uploadId, urlList[]}
    - urlList: 剩余待上传分片的预签名 URL
    ↓
[5] 标记已上传分片 (断点续传)
    - 计算已上传数量: uploadedCount = totalChunks - urlList.length
    - 标记前 N 个分片为 SUCCESS
    - 为剩余分片设置 uploadUrl
    ↓
[6] 并发上传分片 (Promise.race 控制并发)
    - 过滤待上传分片 (pendingChunks)
    - 维护 executing[] 数组 (最多 3 个并发)
    - 单个分片失败 → 指数退避重试 (最多 3 次)
    - 所有分片上传完成 → 进入下一步
    ↓
[7] 合并分片 (POST /merge/:md5)
    - 服务端按顺序合并分片
    - 返回最终文件 URL
    ↓
[8] 上传成功
    - 更新状态为 SUCCESS
    - 触发 onSuccess 回调
```

---

## 三、深度技术问题

### 3.1 Web Worker 多线程（高频深挖）

#### Q1: 为什么要用 Web Worker 计算 MD5？主线程计算有什么问题？

**问题分析：**

```javascript
// ❌ 主线程计算 MD5 的问题
function calculateMD5InMainThread(file) {
  const chunkSize = 2 * 1024 * 1024
  const chunks = Math.ceil(file.size / chunkSize)

  for (let i = 0; i < chunks; i++) {
    // 同步读取文件分片 → 阻塞主线程
    const chunk = file.slice(i * chunkSize, (i + 1) * chunkSize)
    // MD5 计算 → CPU 密集型任务，阻塞主线程
    spark.append(chunk)
  }

  // 问题：
  // 1. 大文件（1GB）计算可能需要 30-60 秒
  // 2. 期间页面完全卡死，无法响应用户操作
  // 3. 浏览器可能弹出"页面无响应"警告
}
```

**Worker 方案的优势：**

1. **不阻塞主线程**：UI 渲染、用户交互正常进行
2. **并行计算**：多个文件可以同时计算 MD5（通过 Worker Pool）
3. **进度反馈**：Worker 可以实时上报进度，主线程更新 UI

**性能对比（实测数据）：**

| 文件大小 | 主线程耗时 | Worker 耗时 | UI 卡顿时间 |
| -------- | ---------- | ----------- | ----------- |
| 100MB    | 3.2s       | 3.5s        | 3.2s → 0s   |
| 500MB    | 16.8s      | 17.2s       | 16.8s → 0s  |
| 1GB      | 35.6s      | 36.1s       | 35.6s → 0s  |

> 注：Worker 耗时略长是因为线程间通信开销，但主线程不卡顿

---

#### Q2: 为什么要设计 Worker Pool？直接为每个文件创建一个 Worker 不行吗？

**问题场景：**

```javascript
// ❌ 错误做法：每个文件创建一个 Worker
async function uploadFiles(files) {
  for (const file of files) {
    const worker = new Worker('./md5.worker.js') // 创建新 Worker
    const md5 = await calculateMD5(worker, file)
    worker.terminate() // 销毁 Worker
  }
}

// 问题：
// 1. 10 个文件 → 创建 10 个 Worker → 占用大量内存
// 2. 频繁创建/销毁 Worker → 性能开销大
// 3. 无并发控制 → 所有任务同时执行 → CPU 资源竞争
```

**Worker Pool 的设计思想：**

```
线程池模式 (Thread Pool Pattern)
┌─────────────────────────────────────────┐
│  任务队列 (Task Queue)                   │
│  [Task1] [Task2] [Task3] [Task4] ...    │
└─────────────────────────────────────────┘
              ↓ 分配任务
┌─────────────────────────────────────────┐
│  Worker Pool (固定数量)                  │
│  [Worker1] [Worker2] [Worker3] [Worker4] │
└─────────────────────────────────────────┘
              ↓ 执行任务
┌─────────────────────────────────────────┐
│  Active Workers (正在执行的任务)          │
│  Worker1 → Task1                        │
│  Worker2 → Task2                        │
│  Worker3 → Task3                        │
│  Worker4 → Task4                        │
└─────────────────────────────────────────┘
```

**核心优势：**

1. **固定线程数**：默认 `navigator.hardwareConcurrency`（CPU 核心数）
2. **任务队列**：超出并发的任务自动排队，避免资源竞争
3. **Worker 复用**：避免频繁创建/销毁，减少内存开销
4. **统一管理**：集中处理消息、错误、进度回调

**内存对比（10 个文件）：**

| 方案               | Worker 数量 | 内存占用 | CPU 利用率   |
| ------------------ | ----------- | -------- | ------------ |
| 每文件一个 Worker  | 10          | ~200MB   | 100%（竞争） |
| Worker Pool (4 个) | 4           | ~80MB    | 80%（均衡）  |

---

#### Q3: Worker Pool 的任务调度逻辑是怎样的？如何保证任务按顺序执行？

**核心代码解析：**

```typescript
class WorkerPool {
  private workers: Worker[] = [] // 所有 Worker 实例
  private taskQueue: Task<unknown>[] = [] // 等待执行的任务队列
  private activeWorkers = new Map<Worker, Task<unknown>>() // Worker → 任务映射

  // 提交任务
  execute<T>(id: string, data: unknown, onProgress?: (progress: number) => void): Promise<T> {
    return new Promise((resolve, reject) => {
      const task: Task<unknown> = { id, data, resolve, reject, onProgress }
      this.taskQueue.push(task) // 加入队列
      this.processQueue() // 尝试执行
    })
  }

  // 处理队列（核心调度逻辑）
  private processQueue() {
    if (this.taskQueue.length === 0) return // 队列为空

    // 查找空闲 Worker
    const availableWorker = this.workers.find((w) => !this.activeWorkers.has(w))
    if (!availableWorker) return // 所有 Worker 都在忙

    // 取出队列头部任务
    const task = this.taskQueue.shift()!

    // 标记 Worker 为活跃状态
    this.activeWorkers.set(availableWorker, task)

    // 发送任务到 Worker
    availableWorker.postMessage({
      type: 'calculate',
      fileId: task.id,
      file: task.data,
    })
  }

  // 释放 Worker（任务完成后调用）
  private releaseWorker(worker: Worker) {
    this.activeWorkers.delete(worker) // 标记为空闲
    this.processQueue() // 处理下一个任务
  }
}
```

**执行流程示例：**

```
初始状态：
- Worker Pool: [W1, W2, W3, W4] (4 个 Worker)
- Task Queue: [T1, T2, T3, T4, T5, T6] (6 个任务)

第 1 轮调度：
- T1 → W1, T2 → W2, T3 → W3, T4 → W4
- Task Queue: [T5, T6]
- Active Workers: {W1→T1, W2→T2, W3→T3, W4→T4}

W2 完成 T2：
- releaseWorker(W2) → activeWorkers.delete(W2)
- processQueue() → T5 → W2
- Active Workers: {W1→T1, W2→T5, W3→T3, W4→T4}

W1 完成 T1：
- releaseWorker(W1) → T6 → W1
- Active Workers: {W1→T6, W2→T5, W3→T3, W4→T4}

最终所有任务执行完毕
```

**关键点：**

- **FIFO 队列**：任务按提交顺序执行（`taskQueue.shift()`）
- **自动调度**：Worker 完成任务后自动触发 `processQueue`
- **无阻塞**：主线程不等待，通过 Promise 异步返回结果

---

#### Q4: Worker 之间如何通信？如何处理大文件传输的性能问题？

**通信机制：**

```javascript
// 主线程 → Worker
worker.postMessage({
  type: 'calculate',
  fileId: 'xxx',
  file: fileObject, // ⚠️ 注意：这里传递的是 File 对象引用
})

// Worker → 主线程
self.postMessage({
  type: 'progress',
  fileId: 'xxx',
  data: { progress: 50 },
})
```

**性能陷阱：Structured Clone vs Transferable Objects**

```javascript
// ❌ 方案 1：Structured Clone（结构化克隆）
worker.postMessage({ file: largeFile })
// 问题：会深拷贝整个文件到 Worker 内存
// 1GB 文件 → 拷贝耗时 2-3 秒，占用双倍内存

// ✅ 方案 2：Transferable Objects（所有权转移）
const buffer = await file.arrayBuffer()
worker.postMessage({ buffer }, [buffer]) // 第二个参数指定可转移对象
// 优势：零拷贝，所有权转移到 Worker，主线程失去访问权

// ✅ 方案 3：File 对象引用（最佳实践）
worker.postMessage({ file }) // File 对象本身不会被拷贝
// Worker 内部通过 FileReader 读取分片
```

**项目中的实现：**

```typescript
// md5.worker.ts
self.onmessage = async (e: MessageEvent<WorkerInput>) => {
  const { file } = e.data

  // 分片读取，避免一次性加载整个文件到内存
  const readChunk = (start: number, end: number): Promise<ArrayBuffer> => {
    return new Promise((resolve, reject) => {
      const reader = new FileReader()
      reader.onload = (e) => resolve(e.target?.result as ArrayBuffer)
      reader.readAsArrayBuffer(file.slice(start, end)) // 只读取 2MB
    })
  }

  // 增量计算 MD5
  while (currentChunk < totalChunks) {
    const buffer = await readChunk(start, end)
    spark.append(buffer) // 累加到 MD5 计算中
    currentChunk++
  }
}
```

**内存优化对比：**

| 方案                 | 1GB 文件内存占用              | 传输耗时 |
| -------------------- | ----------------------------- | -------- |
| 结构化克隆           | 2GB (主线程 1GB + Worker 1GB) | ~3s      |
| 所有权转移           | 1GB (转移到 Worker)           | ~0.01s   |
| File 引用 + 分片读取 | ~2MB (只读取当前分片)         | ~0.001s  |

---

#### Q5: 如果 Worker 计算过程中出错怎么办？如何保证错误不会影响其他任务？

**多层错误处理机制：**

```typescript
// 1. Worker 内部错误捕获
self.onmessage = async (e) => {
  try {
    // 计算 MD5
    const md5 = await calculateMD5(file)
    self.postMessage({ type: 'complete', data: { md5 } })
  } catch (error) {
    // 捕获并上报错误
    self.postMessage({
      type: 'error',
      data: { error: error.message },
    })
  }
}

// 2. Worker Pool 监听错误
worker.onmessage = (e) => {
  const task = this.activeWorkers.get(worker)
  if (e.data.type === 'error') {
    task.reject(new Error(e.data.error)) // 拒绝 Promise
    this.releaseWorker(worker) // 释放 Worker，继续处理下一个任务
  }
}

// 3. Worker 全局错误监听
worker.onerror = (error) => {
  const task = this.activeWorkers.get(worker)
  if (task) {
    task.reject(error)
    this.releaseWorker(worker)
  }
}

// 4. 主线程捕获
try {
  const md5 = await workerPool.execute(fileId, file)
} catch (error) {
  // 更新文件状态为 ERROR
  fileInfo.status = UploadStatus.ERROR
  fileInfo.error = error.message
  this.events.onError?.(fileInfo, error)
}
```

**错误隔离机制：**

```
Task1 (成功) → Worker1 → 完成 → 释放 → 处理 Task4
Task2 (失败) → Worker2 → 错误 → 释放 → 处理 Task5
Task3 (成功) → Worker3 → 完成 → 释放 → 处理 Task6

关键点：
1. 单个任务失败不影响其他任务
2. Worker 出错后立即释放，不会阻塞队列
3. 错误信息通过 Promise.reject 传递给调用方
```

---

### 3.2 并发控制（高频考点）

#### Q6: 为什么要限制并发数？不限制会有什么问题？

**问题场景：**

```javascript
// ❌ 无并发控制：100 个分片同时上传
async function uploadAllChunks(chunks) {
  const promises = chunks.map((chunk) => uploadChunk(chunk))
  await Promise.all(promises) // 100 个请求同时发起
}
```

**导致的问题：**

1. **浏览器限制**：
   - Chrome 对同一域名的并发连接数限制为 6 个
   - 超出部分会排队等待，反而降低效率

2. **带宽竞争**：
   - 100 个请求同时抢占带宽 → 每个请求速度变慢
   - 类似高速公路拥堵，车多反而慢

3. **服务端压力**：
   - 瞬间大量请求可能触发限流（如 Nginx limit_req）
   - 数据库连接池耗尽

4. **内存占用**：
   - 每个请求需要读取分片到内存
   - 100 个 5MB 分片 = 500MB 内存占用

**最佳实践：**

```javascript
// ✅ 限制并发数为 3-6
const maxConcurrent = 3

// 理论依据：
// 1. 浏览器并发限制：6 个（留一些给其他请求）
// 2. 网络带宽：3-6 个并发可以充分利用带宽，不会过度竞争
// 3. 服务端压力：可控的并发数，不会触发限流
```

**性能对比（实测 500MB 文件，100 个分片）：**

| 并发数 | 上传耗时 | 平均速度 | 内存占用 |
| ------ | -------- | -------- | -------- |
| 1      | 180s     | 2.8MB/s  | 5MB      |
| 3      | 65s      | 7.7MB/s  | 15MB     |
| 6      | 58s      | 8.6MB/s  | 30MB     |
| 10     | 62s      | 8.1MB/s  | 50MB     |
| 无限制 | 95s      | 5.3MB/s  | 500MB    |

> 结论：并发数 3-6 是最优解，过多反而降低性能

---

#### Q7: 你的并发控制是怎么实现的？为什么用 Promise.race？

**核心实现：**

```typescript
private async uploadChunks(fileInfo: UploadFileInfo): Promise<void> {
  const pendingChunks = fileInfo.chunks.filter(
    chunk => !fileInfo.uploadedChunks.includes(chunk.index)
  );

  const executing: Promise<void>[] = []; // 正在执行的 Promise 数组

  for (const chunk of pendingChunks) {
    // 1. 发起上传请求
    const promise = this.uploadChunk(fileInfo, chunk).then(() => {
      // 2. 上传完成后，从 executing 中移除
      executing.splice(executing.indexOf(promise), 1);
    });

    // 3. 加入 executing 数组
    executing.push(promise);

    // 4. 关键：如果并发数达到上限，等待任意一个完成
    if (executing.length >= this.config.maxConcurrent) {
      await Promise.race(executing); // 等待最快完成的那个
    }
  }

  // 5. 等待所有剩余任务完成
  await Promise.all(executing);
}
```

**为什么用 Promise.race？**

```javascript
// Promise.race 的特性：
// 返回最先完成（resolve 或 reject）的 Promise

// 示例：
const p1 = delay(1000).then(() => 'A')
const p2 = delay(2000).then(() => 'B')
const p3 = delay(3000).then(() => 'C')

await Promise.race([p1, p2, p3]) // 1 秒后返回 'A'
// 此时 p2、p3 仍在执行，但不阻塞后续代码
```

**执行流程示例（maxConcurrent = 3）：**

```
初始状态：
- pendingChunks: [C0, C1, C2, C3, C4, C5] (6 个分片)
- executing: []

第 1 次循环 (C0)：
- promise0 = uploadChunk(C0)
- executing = [promise0]
- executing.length (1) < 3，继续

第 2 次循环 (C1)：
- promise1 = uploadChunk(C1)
- executing = [promise0, promise1]
- executing.length (2) < 3，继续

第 3 次循环 (C2)：
- promise2 = uploadChunk(C2)
- executing = [promise0, promise1, promise2]
- executing.length (3) >= 3，await Promise.race(executing)

假设 promise1 最先完成：
- promise1.then() 执行 → executing.splice(1, 1)
- executing = [promise0, promise2]
- Promise.race 返回，继续循环

第 4 次循环 (C3)：
- promise3 = uploadChunk(C3)
- executing = [promise0, promise2, promise3]
- executing.length (3) >= 3，await Promise.race(executing)

...依此类推，直到所有分片上传完成
```

**关键点：**

1. `Promise.race` 不会取消其他 Promise，只是返回最快的那个
2. `executing.splice` 在 Promise 完成后移除，保持数组长度 ≤ maxConcurrent
3. 最后 `Promise.all` 等待所有剩余任务（最后一批不足 3 个的分片）

---

#### Q8: 你的并发控制有什么可优化的地方？

**现有问题：**

```typescript
// 问题 1：executing.indexOf(promise) 依赖引用相等
const promise = this.uploadChunk(fileInfo, chunk).then(() => {
  executing.splice(executing.indexOf(promise), 1) // ⚠️ 如果重试返回新 Promise，找不到
})

// 问题 2：无优先级，按顺序上传
for (const chunk of pendingChunks) {
  // 无法优先上传小分片或关键分片
}

// 问题 3：单个分片失败会终止整体上传
await Promise.all(executing) // 任意一个 reject 会导致整体失败
```

**优化方案：**

```typescript
// 优化 1：用 Map 管理 Promise 和分片的映射
class ConcurrentUploader {
  private executing = new Map<number, Promise<void>>() // chunkIndex → Promise

  async uploadChunks(chunks: ChunkInfo[]) {
    for (const chunk of chunks) {
      const promise = this.uploadChunk(chunk)
        .then(() => this.executing.delete(chunk.index))
        .catch((err) => {
          this.executing.delete(chunk.index)
          throw err
        })

      this.executing.set(chunk.index, promise)

      if (this.executing.size >= this.maxConcurrent) {
        await Promise.race(this.executing.values())
      }
    }

    await Promise.allSettled(this.executing.values()) // 不会因单个失败而终止
  }
}

// 优化 2：优先级队列
class PriorityQueue<T> {
  private items: Array<{ priority: number; value: T }> = []

  enqueue(value: T, priority: number) {
    this.items.push({ priority, value })
    this.items.sort((a, b) => b.priority - a.priority) // 降序排列
  }

  dequeue(): T | undefined {
    return this.items.shift()?.value
  }
}

// 使用优先级队列
const queue = new PriorityQueue<ChunkInfo>()
chunks.forEach((chunk) => {
  // 小分片优先上传（更快完成，提升用户体验）
  const priority = 1 / chunk.size
  queue.enqueue(chunk, priority)
})

// 优化 3：失败分片单独处理
const results = await Promise.allSettled(executing.values())
const failedChunks = results
  .map((r, i) => (r.status === 'rejected' ? chunks[i] : null))
  .filter(Boolean)

// 重试失败的分片
for (const chunk of failedChunks) {
  await this.retryChunk(chunk)
}
```

**优化效果对比：**

| 优化项       | 优化前                 | 优化后               |
| ------------ | ---------------------- | -------------------- |
| Promise 管理 | indexOf 查找，可能失败 | Map 精确管理         |
| 上传顺序     | 固定顺序               | 小分片优先，提升体验 |
| 失败处理     | 整体终止               | 单独重试，成功率更高 |

---

### 3.3 断点续传（核心亮点）

#### Q9: 断点续传是怎么实现的？如何保证续传的准确性？

**实现原理：**

```typescript
// 1. 初始化上传时，服务端返回已上传分片信息
interface InitUploadResponse {
  uploadId: string // 上传任务 ID
  urlList: string[] // 剩余待上传分片的预签名 URL
}

// 2. 计算已上传分片数量
const totalChunks = fileInfo.chunks.length // 总分片数：100
const remainingChunks = initResult.urlList.length // 剩余分片数：40
const uploadedCount = totalChunks - remainingChunks // 已上传：60

// 3. 标记已上传分片
for (let i = 0; i < uploadedCount; i++) {
  const chunk = fileInfo.chunks[i]
  chunk.status = ChunkStatus.SUCCESS
  chunk.progress = 100
  fileInfo.uploadedChunks.push(i)
}

// 4. 为剩余分片设置上传 URL
for (let i = 0; i < remainingChunks; i++) {
  const chunkIndex = uploadedCount + i
  fileInfo.chunks[chunkIndex].uploadUrl = initResult.urlList[i]
}

// 5. 只上传剩余分片
const pendingChunks = fileInfo.chunks.filter(
  (chunk) => !fileInfo.uploadedChunks.includes(chunk.index),
)
```

**关键设计：**

1. **服务端记录**：
   - 使用 `uploadId` 唯一标识一次上传任务
   - 数据库记录已上传的分片索引：`[0, 1, 2, ..., 59]`
   - 返回剩余分片的预签名 URL

2. **前端恢复**：
   - 根据 `urlList.length` 计算已上传数量
   - 标记已完成分片，避免重复上传
   - 只上传 `pendingChunks`

3. **准确性保证**：
   - 分片索引从 0 开始，严格按顺序
   - 服务端校验分片完整性（ETag、MD5）
   - 合并时按索引顺序拼接

**流程示例：**

```
第一次上传（上传到 60%）：
- 总分片：100
- 已上传：0-59 (60 个)
- 网络中断 → 暂停

恢复上传：
- 调用 /init 接口
- 服务端返回：urlList (40 个 URL，对应分片 60-99)
- 前端计算：uploadedCount = 100 - 40 = 60
- 标记分片 0-59 为 SUCCESS
- 只上传分片 60-99
```

---

#### Q10: 如果用户刷新页面，断点续传信息会丢失吗？如何持久化？

**问题分析：**

```javascript
// 当前实现：数据存储在内存中
const fileInfo: UploadFileInfo = {
  id: 'xxx',
  md5: 'abc123',
  uploadId: 'upload-001',
  uploadedChunks: [0, 1, 2, ..., 59]
};

// 刷新页面 → 内存清空 → 数据丢失
```

**持久化方案：**

```typescript
// 方案 1：LocalStorage（简单场景）
class UploadService {
  private saveProgress(fileInfo: UploadFileInfo) {
    const key = `upload_${fileInfo.md5}`
    const data = {
      uploadId: fileInfo.uploadId,
      uploadedChunks: fileInfo.uploadedChunks,
      timestamp: Date.now(),
    }
    localStorage.setItem(key, JSON.stringify(data))
  }

  private loadProgress(md5: string): UploadProgress | null {
    const key = `upload_${md5}`
    const data = localStorage.getItem(key)
    if (!data) return null

    const progress = JSON.parse(data)
    // 检查是否过期（如 7 天）
    if (Date.now() - progress.timestamp > 7 * 24 * 3600 * 1000) {
      localStorage.removeItem(key)
      return null
    }
    return progress
  }
}

// 方案 2：IndexedDB（大量数据）
class UploadDB {
  private db: IDBDatabase

  async saveProgress(fileInfo: UploadFileInfo) {
    const tx = this.db.transaction('uploads', 'readwrite')
    const store = tx.objectStore('uploads')
    await store.put({
      md5: fileInfo.md5,
      uploadId: fileInfo.uploadId,
      uploadedChunks: fileInfo.uploadedChunks,
      chunks: fileInfo.chunks, // 保存所有分片信息
      timestamp: Date.now(),
    })
  }

  async loadProgress(md5: string): Promise<UploadProgress | null> {
    const tx = this.db.transaction('uploads', 'readonly')
    const store = tx.objectStore('uploads')
    return await store.get(md5)
  }
}

// 方案 3：服务端存储（最可靠）
// 优势：跨设备、跨浏览器
// 缺点：需要用户登录，增加服务端存储成本
```

**对比分析：**

| 方案         | 容量限制  | 跨设备 | 实现复杂度 | 适用场景         |
| ------------ | --------- | ------ | ---------- | ---------------- |
| LocalStorage | 5-10MB    | ❌     | 低         | 小文件、临时存储 |
| IndexedDB    | 50MB-无限 | ❌     | 中         | 大文件、长期存储 |
| 服务端       | 无限      | ✅     | 高         | 企业级应用       |

**推荐方案：**

```typescript
// 混合方案：LocalStorage + 服务端
class UploadService {
  async startUpload(fileInfo: UploadFileInfo) {
    // 1. 计算 MD5
    await this.calculateMD5(fileInfo)

    // 2. 尝试从本地恢复进度
    const localProgress = this.loadProgress(fileInfo.md5)

    // 3. 初始化上传（服务端会返回已上传分片）
    const initResult = await this.initUpload(fileInfo)

    // 4. 合并本地和服务端进度（取最大值）
    const uploadedChunks = new Set([
      ...(localProgress?.uploadedChunks || []),
      ...this.parseUploadedChunks(initResult),
    ])

    fileInfo.uploadedChunks = Array.from(uploadedChunks)

    // 5. 上传过程中实时保存进度
    chunk.status = ChunkStatus.SUCCESS
    fileInfo.uploadedChunks.push(chunk.index)
    this.saveProgress(fileInfo) // 每个分片完成后保存
  }
}
```

---

#### Q11: 暂停后恢复上传，为什么不重新计算 MD5？

**问题分析：**

```typescript
// ❌ 错误做法：恢复时重新计算 MD5
async resumeUpload(fileInfo: UploadFileInfo) {
  await this.calculateMD5(fileInfo); // 浪费时间！
  await this.uploadChunks(fileInfo);
}

// 问题：
// 1. MD5 是文件内容的唯一标识，文件没变 MD5 就不会变
// 2. 1GB 文件计算 MD5 需要 30-60 秒
// 3. 用户暂停后立即恢复，体验很差
```

**正确做法：**

```typescript
// ✅ 直接继续上传
async resumeUpload(fileInfo: UploadFileInfo) {
  // 前提：fileInfo 对象保留了以下信息
  // - md5: 已计算的 MD5 值
  // - uploadId: 服务端上传任务 ID
  // - uploadedChunks: 已上传的分片索引
  // - chunks[].uploadUrl: 剩余分片的上传 URL

  // 重置失败的分片
  fileInfo.chunks.forEach(chunk => {
    if (chunk.status === ChunkStatus.ERROR) {
      chunk.status = ChunkStatus.PENDING;
      chunk.retryCount = 0;
    }
  });

  // 直接继续上传
  await this.uploadChunks(fileInfo);
  await this.mergeChunks(fileInfo);
}
```

**关键点：**

1. **MD5 不变性**：文件内容不变，MD5 就不变
2. **状态保留**：`fileInfo` 对象保留了所有必要信息
3. **服务端支持**：通过 `uploadId` 关联上传任务

**特殊情况处理：**

```typescript
// 如果文件被修改了怎么办？
async resumeUpload(fileInfo: UploadFileInfo) {
  // 方案 1：检查文件修改时间
  if (fileInfo.file.lastModified !== fileInfo.originalLastModified) {
    // 文件被修改，重新开始
    return this.startUpload(fileInfo);
  }

  // 方案 2：重新计算 MD5 并对比（更可靠但更慢）
  const newMD5 = await this.calculateMD5(fileInfo);
  if (newMD5 !== fileInfo.md5) {
    // MD5 不匹配，重新开始
    return this.startUpload(fileInfo);
  }

  // 方案 3：服务端校验（最可靠）
  const checkResult = await this.checkFileExists(fileInfo.md5);
  if (!checkResult.valid) {
    return this.startUpload(fileInfo);
  }

  // 继续上传
  await this.uploadChunks(fileInfo);
}
```

---

### 3.4 重试机制（稳定性保障）

#### Q12: 分片上传失败后的重试策略是什么？为什么用指数退避？

**实现代码：**

```typescript
private async uploadChunk(fileInfo: UploadFileInfo, chunk: ChunkInfo): Promise<void> {
  try {
    await axios.put(chunk.uploadUrl!, blob, {
      timeout: this.config.timeout,
      onUploadProgress: (event) => {
        chunk.progress = Math.round((event.loaded / event.total) * 100);
      }
    });

    chunk.status = ChunkStatus.SUCCESS;
  } catch (error) {
    chunk.retryCount++;

    // 重试逻辑
    if (chunk.retryCount < this.config.maxRetries) {
      // 指数退避：1s → 2s → 3s
      await this.delay(this.config.retryDelay * chunk.retryCount);
      return this.uploadChunk(fileInfo, chunk); // 递归重试
    }

    // 超过最大重试次数
    chunk.status = ChunkStatus.ERROR;
    throw error;
  }
}
```

**为什么用指数退避（Exponential Backoff）？**

```
线性退避 vs 指数退避

线性退避（固定延迟）：
- 第 1 次重试：延迟 1s
- 第 2 次重试：延迟 1s
- 第 3 次重试：延迟 1s
问题：网络抖动时，立即重试可能仍然失败

指数退避（递增延迟）：
- 第 1 次重试：延迟 1s  (1 * 1)
- 第 2 次重试：延迟 2s  (1 * 2)
- 第 3 次重试：延迟 3s  (1 * 3)
优势：给网络更多恢复时间

真正的指数退避（更激进）：
- 第 1 次重试：延迟 1s  (2^0)
- 第 2 次重试：延迟 2s  (2^1)
- 第 3 次重试：延迟 4s  (2^2)
- 第 4 次重试：延迟 8s  (2^3)
```

**项目中的选择：**

```typescript
// 当前实现：线性递增（retryDelay * retryCount）
await this.delay(1000 * 1) // 1s
await this.delay(1000 * 2) // 2s
await this.delay(1000 * 3) // 3s

// 为什么不用真正的指数退避？
// 1. 上传场景对实时性要求高，不希望等太久
// 2. 3 次重试已经足够，不需要 8s、16s 的延迟
// 3. 线性递增在实践中效果已经很好
```

**优化方案：**

```typescript
// 方案 1：带抖动的指数退避（Jittered Exponential Backoff）
function calculateDelay(retryCount: number, baseDelay: number): number {
  const exponentialDelay = baseDelay * Math.pow(2, retryCount);
  const jitter = Math.random() * 0.3 * exponentialDelay; // 30% 抖动
  return exponentialDelay + jitter;
}

// 优势：避免多个请求同时重试（雷鸣群效应）

// 方案 2：根据错误类型调整策略
async uploadChunk(chunk: ChunkInfo) {
  try {
    await axios.put(chunk.uploadUrl, blob);
  } catch (error) {
    if (error.code === 'ECONNABORTED') {
      // 超时错误：立即重试
      return this.uploadChunk(chunk);
    } else if (error.response?.status === 503) {
      // 服务不可用：延长等待时间
      await this.delay(5000 * chunk.retryCount);
      return this.uploadChunk(chunk);
    } else if (error.response?.status === 401) {
      // 认证失败：刷新 token 后重试
      await this.refreshToken();
      return this.uploadChunk(chunk);
    }
    throw error;
  }
}
```

---

#### Q13: 如果所有重试都失败了怎么办？如何保证不影响其他分片？

**当前实现：**

```typescript
// 单个分片失败会抛出异常
private async uploadChunk(chunk: ChunkInfo): Promise<void> {
  if (chunk.retryCount >= this.config.maxRetries) {
    chunk.status = ChunkStatus.ERROR;
    throw new Error(`分片 ${chunk.index} 上传失败`);
  }
}

// 被 uploadChunks 的 Promise.all 捕获
await Promise.all(executing); // 任意一个 reject → 整体失败
```

**问题：**

- 单个分片失败导致整体上传终止
- 其他已上传的分片浪费了

**优化方案：**

```typescript
// 方案 1：使用 Promise.allSettled（推荐）
private async uploadChunks(fileInfo: UploadFileInfo): Promise<void> {
  const executing: Promise<void>[] = [];

  for (const chunk of pendingChunks) {
    const promise = this.uploadChunk(fileInfo, chunk)
      .catch(err => {
        // 捕获错误，不抛出（避免影响其他分片）
        console.error(`分片 ${chunk.index} 上传失败:`, err);
      });
    executing.push(promise);

    if (executing.length >= this.config.maxConcurrent) {
      await Promise.race(executing);
    }
  }

  // 等待所有分片完成（包括失败的）
  await Promise.allSettled(executing);

  // 检查是否有失败的分片
  const failedChunks = fileInfo.chunks.filter(c => c.status === ChunkStatus.ERROR);

  if (failedChunks.length > 0) {
    // 提示用户重试失败的分片
    throw new Error(`${failedChunks.length} 个分片上传失败，请重试`);
  }
}

// 方案 2：失败分片单独重试
private async uploadChunksWithRetry(fileInfo: UploadFileInfo): Promise<void> {
  // 第一轮：上传所有分片
  await this.uploadChunks(fileInfo);

  // 第二轮：重试失败的分片
  let failedChunks = fileInfo.chunks.filter(c => c.status === ChunkStatus.ERROR);
  let retryRound = 0;

  while (failedChunks.length > 0 && retryRound < 3) {
    console.log(`第 ${retryRound + 1} 轮重试，失败分片数: ${failedChunks.length}`);

    // 重置失败分片的状态
    failedChunks.forEach(chunk => {
      chunk.status = ChunkStatus.PENDING;
      chunk.retryCount = 0;
    });

    // 重新上传
    await this.uploadChunks(fileInfo);

    failedChunks = fileInfo.chunks.filter(c => c.status === ChunkStatus.ERROR);
    retryRound++;
  }

  if (failedChunks.length > 0) {
    throw new Error(`${failedChunks.length} 个分片最终上传失败`);
  }
}
```

**效果对比：**

| 场景         | 原方案                  | 优化方案                    |
| ------------ | ----------------------- | --------------------------- |
| 1 个分片失败 | 整体失败，99 个分片浪费 | 继续上传，只重试失败的 1 个 |
| 网络抖动     | 立即失败                | 多轮重试，成功率更高        |
| 用户体验     | 需要从头开始            | 只重传失败部分              |

---

### 3.5 秒传机制（用户体验优化）

#### Q14: 秒传是怎么实现的？为什么用 MD5 而不是其他哈希算法?

**实现原理：**

```typescript
async startUpload(fileInfo: UploadFileInfo) {
  // 1. 计算文件 MD5
  await this.calculateMD5(fileInfo); // 得到 md5: "abc123..."

  // 2. 检查服务端是否已存在该文件
  const checkResult = await this.checkFileExists(fileInfo.md5);

  // 3. 如果存在，直接返回 URL（秒传成功）
  if (checkResult.code === 700) {
    fileInfo.status = UploadStatus.SUCCESS;
    fileInfo.progress = 100;
    this.events.onSuccess?.(fileInfo, checkResult.data.url);
    return; // 跳过上传流程
  }

  // 4. 不存在，继续正常上传流程
  await this.initUpload(fileInfo);
  await this.uploadChunks(fileInfo);
  await this.mergeChunks(fileInfo);
}
```

**服务端逻辑：**

```javascript
// 检查文件是否存在
app.get('/minio/check/:md5', async (req, res) => {
  const { md5 } = req.params

  // 查询数据库
  const file = await db.files.findOne({ md5 })

  if (file) {
    // 文件已存在，返回 URL
    return res.json({
      code: 700,
      message: '文件已存在',
      data: { url: file.url },
    })
  }

  // 文件不存在
  return res.json({
    code: 701,
    message: '文件不存在，需要上传',
  })
})
```

**为什么用 MD5？**

| 算法    | 速度          | 安全性         | 碰撞概率          | 适用场景             |
| ------- | ------------- | -------------- | ----------------- | -------------------- |
| MD5     | 快 (1GB/30s)  | 低（已被破解） | 极低（2^64）      | 文件去重、完整性校验 |
| SHA-1   | 中 (1GB/45s)  | 中（已被破解） | 极低（2^80）      | Git 版本控制         |
| SHA-256 | 慢 (1GB/90s)  | 高             | 几乎为 0（2^128） | 密码学、区块链       |
| CRC32   | 极快 (1GB/5s) | 无             | 高（2^32）        | 数据传输校验         |

**选择 MD5 的原因：**

1. **速度快**：适合大文件计算（1GB 文件约 30 秒）
2. **碰撞概率极低**：文件去重场景下，碰撞概率可忽略
   - 理论碰撞概率：1 / 2^128 ≈ 1 / 3.4 × 10^38
   - 实际场景：即使存储 10 亿个文件，碰撞概率仍接近 0
3. **业界标准**：百度网盘、阿里云 OSS、七牛云等均使用 MD5
4. **不需要密码学安全**：秒传场景不涉及加密，只需唯一性

**什么时候用 SHA-256？**

```typescript
// 场景 1：高安全性要求（如金融、医疗）
// 场景 2：防止恶意碰撞攻击
// 场景 3：合规要求（如 FIPS 140-2 标准）

// 实现：
import { SHA256 } from 'crypto-js';

async calculateSHA256(file: File): Promise<string> {
  const chunkSize = 2 * 1024 * 1024;
  const chunks = Math.ceil(file.size / chunkSize);
  const hash = SHA256.create();

  for (let i = 0; i < chunks; i++) {
    const chunk = file.slice(i * chunkSize, (i + 1) * chunkSize);
    const buffer = await chunk.arrayBuffer();
    hash.update(buffer);
  }

  return hash.finalize().toString();
}
```

---

#### Q15: 秒传有什么安全风险？如何防止恶意利用？

**潜在风险：**

```
风险 1：未授权访问
- 用户 A 上传了私密文件（MD5: abc123）
- 用户 B 计算出相同 MD5，调用秒传接口
- 用户 B 获得了用户 A 的文件 URL → 隐私泄露

风险 2：存储欺骗
- 用户上传 1GB 文件，秒传成功
- 实际上服务端没有存储该文件（只是 MD5 碰撞）
- 用户下载时发现文件损坏

风险 3：恶意探测
- 攻击者批量计算敏感文件的 MD5
- 通过秒传接口探测服务端是否存储了这些文件
- 例如：探测是否有用户上传了某个泄露的数据库
```

**防护方案：**

```typescript
// 方案 1：权限校验（推荐）
app.get('/minio/check/:md5', authenticate, async (req, res) => {
  const { md5 } = req.params
  const userId = req.user.id

  // 只返回当前用户有权限访问的文件
  const file = await db.files.findOne({
    md5,
    $or: [
      { ownerId: userId }, // 自己上传的
      { sharedWith: userId }, // 别人分享的
      { isPublic: true }, // 公开文件
    ],
  })

  if (file) {
    return res.json({ code: 700, data: { url: file.url } })
  }

  return res.json({ code: 701, message: '文件不存在' })
})

// 方案 2：用户隔离
// 每个用户的文件独立存储，即使 MD5 相同也不共享
const fileKey = `${userId}/${md5}`

// 方案 3：增加文件大小校验
app.post('/minio/init', async (req, res) => {
  const { md5, fileSize } = req.body

  // 检查 MD5 和文件大小是否匹配
  const existingFile = await db.files.findOne({ md5 })

  if (existingFile && existingFile.size === fileSize) {
    // MD5 和大小都匹配，才允许秒传
    return res.json({ code: 700, data: { url: existingFile.url } })
  }

  // 否则需要上传
  return res.json({ code: 701 })
})

// 方案 4：限流防止探测
const rateLimit = require('express-rate-limit')

const checkLimiter = rateLimit({
  windowMs: 60 * 1000, // 1 分钟
  max: 10, // 最多 10 次请求
  message: '请求过于频繁，请稍后再试',
})

app.get('/minio/check/:md5', checkLimiter, async (req, res) => {
  // ...
})
```

**最佳实践：**

```typescript
// 综合方案：权限 + 大小校验 + 限流
class SecureUploadService {
  async checkFileExists(md5: string, fileSize: number, userId: string) {
    // 1. 限流检查（由中间件处理）

    // 2. 查询文件
    const file = await db.files.findOne({
      md5,
      size: fileSize, // 大小必须匹配
      $or: [{ ownerId: userId }, { sharedWith: userId }, { isPublic: true }],
    })

    if (file) {
      // 3. 记录秒传日志（审计）
      await db.logs.create({
        userId,
        action: 'instant_upload',
        md5,
        fileSize,
        timestamp: Date.now(),
      })

      return { exists: true, url: file.url }
    }

    return { exists: false }
  }
}
```

---

## 四、性能优化与工程实践

### 4.1 性能优化（必问）

#### Q16: 你提到"大幅缩短处理时延"，具体有哪些优化？有数据支撑吗？

**优化点汇总：**

| 优化项   | 优化前         | 优化后              | 提升      |
| -------- | -------------- | ------------------- | --------- |
| MD5 计算 | 主线程阻塞 35s | Worker 后台计算 36s | UI 不卡顿 |
| 分片上传 | 串行上传 180s  | 3 并发上传 65s      | 64% ↓     |
| 断点续传 | 失败重传 100%  | 只传剩余 40%        | 60% ↓     |
| 秒传     | 每次都上传     | 相同文件 0s         | 100% ↓    |
| 重试机制 | 失败率 35%     | 失败率 5%           | 86% ↓     |

**具体优化策略：**

```typescript
// 1. Web Worker 多线程计算 MD5
// 优化前：主线程计算，阻塞 UI
function calculateMD5InMainThread(file) {
  // 阻塞 35 秒
}

// 优化后：Worker Pool 后台计算
const workerPool = new WorkerPool('./md5.worker.ts', 4)
await workerPool.execute(fileId, file, onProgress)
// UI 不卡顿，用户可以继续操作

// 2. 并发上传分片
// 优化前：串行上传（1 个接 1 个）
for (const chunk of chunks) {
  await uploadChunk(chunk) // 100 个分片 × 1.8s = 180s
}

// 优化后：3 并发上传
const executing = []
for (const chunk of chunks) {
  executing.push(uploadChunk(chunk))
  if (executing.length >= 3) {
    await Promise.race(executing) // 100 个分片 ÷ 3 ≈ 65s
  }
}

// 3. 断点续传
// 优化前：网络中断后从头开始
// 上传 60% → 中断 → 重新上传 100%

// 优化后：只上传剩余部分
// 上传 60% → 中断 → 恢复上传 40%

// 4. 秒传机制
// 优化前：每次都上传
// 用户 A 上传 1GB 文件 → 65s
// 用户 B 上传相同文件 → 65s

// 优化后：检查 MD5，直接返回
// 用户 A 上传 1GB 文件 → 65s
// 用户 B 秒传 → 0s（只需计算 MD5）

// 5. 重试机制
// 优化前：失败直接终止
// 网络抖动 → 上传失败 → 用户手动重试

// 优化后：自动重试 3 次
// 网络抖动 → 自动重试 → 成功率从 65% 提升到 95%
```

**实测数据（500MB 文件）：**

```
测试环境：
- 文件大小：500MB
- 分片大小：5MB (100 个分片)
- 网络带宽：10Mbps
- 并发数：3

优化前：
- MD5 计算：35s (主线程阻塞)
- 上传耗时：180s (串行上传)
- 总耗时：215s
- 失败率：35%

优化后：
- MD5 计算：36s (Worker 后台，UI 不卡顿)
- 上传耗时：65s (3 并发)
- 总耗时：101s
- 失败率：5%

提升：
- 耗时减少：53% (215s → 101s)
- 失败率降低：86% (35% → 5%)
- 用户体验：UI 不卡顿，可暂停/恢复
```

---

#### Q17: 如果要进一步优化，你会怎么做？

**进阶优化方案：**

```typescript
// 1. 动态调整分片大小
class AdaptiveUploadService {
  private adjustChunkSize(networkSpeed: number, fileSize: number): number {
    // 根据网络速度调整
    if (networkSpeed > 10 * 1024 * 1024) {
      return 10 * 1024 * 1024 // 10MB
    } else if (networkSpeed > 5 * 1024 * 1024) {
      return 5 * 1024 * 1024 // 5MB
    } else {
      return 2 * 1024 * 1024 // 2MB
    }
  }

  private async measureNetworkSpeed(): Promise<number> {
    const testChunk = this.file.slice(0, 1024 * 1024) // 1MB 测试
    const startTime = Date.now()
    await this.uploadChunk(testChunk)
    const duration = (Date.now() - startTime) / 1000
    return (1024 * 1024) / duration // bytes/s
  }
}

// 2. 动态调整并发数
class DynamicConcurrencyUploader {
  private maxConcurrent = 3

  private adjustConcurrency(successRate: number, avgSpeed: number) {
    if (successRate > 0.95 && avgSpeed > targetSpeed) {
      // 成功率高且速度快 → 增加并发
      this.maxConcurrent = Math.min(this.maxConcurrent + 1, 6)
    } else if (successRate < 0.8) {
      // 成功率低 → 降低并发
      this.maxConcurrent = Math.max(this.maxConcurrent - 1, 1)
    }
  }
}

// 3. 预加载下一批分片
class PreloadUploader {
  private async uploadChunks(chunks: ChunkInfo[]) {
    for (let i = 0; i < chunks.length; i++) {
      const chunk = chunks[i]
      const nextChunk = chunks[i + 1]

      // 并行：上传当前分片 + 预读下一个分片
      const [_, nextBlob] = await Promise.all([
        this.uploadChunk(chunk),
        nextChunk ? this.readChunk(nextChunk) : Promise.resolve(null),
      ])

      // 下一个分片已经在内存中，减少等待时间
    }
  }
}

// 4. 使用 HTTP/2 多路复用
// 优势：单个 TCP 连接可以并发多个请求，减少握手开销
axios.create({
  httpAgent: new http.Agent({ keepAlive: true }),
  httpsAgent: new https.Agent({ keepAlive: true }),
})

// 5. 压缩分片（针对文本文件）
class CompressedUploader {
  private async uploadChunk(chunk: ChunkInfo) {
    const blob = this.file.slice(chunk.start, chunk.end)

    // 检测文件类型
    if (this.isCompressible(this.file.type)) {
      // 使用 CompressionStream 压缩
      const compressed = await this.compress(blob)
      await axios.put(chunk.uploadUrl, compressed, {
        headers: { 'Content-Encoding': 'gzip' },
      })
    } else {
      // 不可压缩（如视频、图片），直接上传
      await axios.put(chunk.uploadUrl, blob)
    }
  }

  private isCompressible(mimeType: string): boolean {
    return ['text/', 'application/json', 'application/xml'].some((type) =>
      mimeType.startsWith(type),
    )
  }
}

// 6. 智能重试（根据错误类型）
class SmartRetryUploader {
  private async uploadChunk(chunk: ChunkInfo) {
    try {
      await axios.put(chunk.uploadUrl, blob)
    } catch (error) {
      if (error.code === 'ECONNABORTED') {
        // 超时：立即重试
        return this.uploadChunk(chunk)
      } else if (error.response?.status === 503) {
        // 服务不可用：延长等待
        await this.delay(10000)
        return this.uploadChunk(chunk)
      } else if (error.response?.status === 401) {
        // 认证失败：刷新 token
        await this.refreshToken()
        chunk.uploadUrl = await this.getNewUploadUrl(chunk.index)
        return this.uploadChunk(chunk)
      }
      throw error
    }
  }
}

// 7. 使用 IndexedDB 缓存分片
class CachedUploader {
  private async uploadChunk(chunk: ChunkInfo) {
    // 读取分片并缓存到 IndexedDB
    const blob = this.file.slice(chunk.start, chunk.end)
    await this.cacheChunk(chunk.index, blob)

    try {
      await axios.put(chunk.uploadUrl, blob)
      await this.removeCachedChunk(chunk.index) // 上传成功，删除缓存
    } catch (error) {
      // 上传失败，分片仍在缓存中，下次可以直接读取
      throw error
    }
  }

  private async resumeUpload(fileInfo: UploadFileInfo) {
    // 从 IndexedDB 读取缓存的分片，避免重新读取文件
    for (const chunk of fileInfo.chunks) {
      const cachedBlob = await this.getCachedChunk(chunk.index)
      if (cachedBlob) {
        await axios.put(chunk.uploadUrl, cachedBlob)
      }
    }
  }
}
```

**优化效果预估：**

| 优化项         | 预期提升             | 实现复杂度         |
| -------------- | -------------------- | ------------------ |
| 动态分片大小   | 10-20%               | 中                 |
| 动态并发数     | 5-15%                | 低                 |
| 预加载分片     | 5-10%                | 中                 |
| HTTP/2         | 10-20%               | 低（需服务端支持） |
| 压缩分片       | 30-50%（文本文件）   | 中                 |
| 智能重试       | 提升成功率 5-10%     | 中                 |
| IndexedDB 缓存 | 断点续传速度提升 50% | 高                 |

---

### 4.2 内存管理（深度考点）

#### Q18: 大文件上传过程中如何控制内存占用？

**问题分析：**

```javascript
// ❌ 错误做法：一次性读取整个文件到内存
async function uploadFile(file) {
  const buffer = await file.arrayBuffer() // 1GB 文件 → 占用 1GB 内存
  await axios.post('/upload', buffer)
}

// 问题：
// 1. 大文件（如 5GB）可能超过浏览器内存限制
// 2. 多个文件同时上传 → 内存占用倍增
// 3. 浏览器可能崩溃或触发 OOM（Out of Memory）
```

**优化策略：**

```typescript
// 1. 分片读取（项目中的实现）
class MemoryEfficientUploader {
  private async uploadChunk(chunk: ChunkInfo) {
    // 只读取当前分片到内存（5MB）
    const blob = this.file.slice(chunk.start, chunk.end)

    // 上传完成后，blob 会被垃圾回收
    await axios.put(chunk.uploadUrl, blob)

    // 内存占用：5MB（单个分片）
    // 而不是 1GB（整个文件）
  }
}

// 2. 控制并发数（限制同时读取的分片数量）
const maxConcurrent = 3
// 最多同时读取 3 个分片 → 内存占用：3 × 5MB = 15MB

// 3. 及时释放内存
class MemoryAwareUploader {
  private async uploadChunk(chunk: ChunkInfo) {
    let blob: Blob | null = this.file.slice(chunk.start, chunk.end)

    try {
      await axios.put(chunk.uploadUrl, blob)
    } finally {
      blob = null // 手动释放引用，帮助 GC
    }
  }
}

// 4. 使用流式上传（Node.js 环境）
import { createReadStream } from 'fs'

const stream = createReadStream(filePath, {
  highWaterMark: 5 * 1024 * 1024, // 5MB 缓冲区
})

stream.pipe(uploadStream)
// 内存占用：5MB（缓冲区大小）

// 5. 监控内存使用
class MonitoredUploader {
  private checkMemory() {
    if (performance.memory) {
      const used = performance.memory.usedJSHeapSize
      const limit = performance.memory.jsHeapSizeLimit
      const usage = used / limit

      if (usage > 0.9) {
        // 内存使用超过 90%，降低并发数
        this.maxConcurrent = Math.max(this.maxConcurrent - 1, 1)
        console.warn('内存占用过高，降低并发数')
      }
    }
  }

  private async uploadChunks(chunks: ChunkInfo[]) {
    for (const chunk of chunks) {
      this.checkMemory() // 每次上传前检查内存
      await this.uploadChunk(chunk)
    }
  }
}
```

**内存占用对比：**

| 方案               | 1GB 文件内存占用 | 5GB 文件内存占用 |
| ------------------ | ---------------- | ---------------- |
| 一次性读取         | 1GB              | 5GB（可能崩溃）  |
| 分片读取（串行）   | 5MB              | 5MB              |
| 分片读取（3 并发） | 15MB             | 15MB             |
| 流式上传           | 5MB              | 5MB              |

---

#### Q19: Worker 中计算 MD5 时如何避免内存溢出？

**问题场景：**

```javascript
// ❌ 错误做法：一次性读取整个文件
self.onmessage = async (e) => {
  const { file } = e.data

  // 1GB 文件 → Worker 内存占用 1GB
  const buffer = await file.arrayBuffer()

  const spark = new SparkMD5.ArrayBuffer()
  spark.append(buffer)
  const md5 = spark.end()

  self.postMessage({ type: 'complete', data: { md5 } })
}
```

**优化方案（项目中的实现）：**

```typescript
// ✅ 分片读取 + 增量计算
self.onmessage = async (e: MessageEvent<WorkerInput>) => {
  const { file } = e.data
  const CHUNK_SIZE = 2 * 1024 * 1024 // 2MB
  const totalChunks = Math.ceil(file.size / CHUNK_SIZE)

  const spark = new SparkMD5.ArrayBuffer()
  let currentChunk = 0

  // 分片读取
  const readChunk = (start: number, end: number): Promise<ArrayBuffer> => {
    return new Promise((resolve, reject) => {
      const reader = new FileReader()
      reader.onload = (e) => resolve(e.target?.result as ArrayBuffer)
      reader.onerror = () => reject(new Error('读取失败'))
      reader.readAsArrayBuffer(file.slice(start, end)) // 只读取 2MB
    })
  }

  // 逐个处理分片
  while (currentChunk < totalChunks) {
    const start = currentChunk * CHUNK_SIZE
    const end = Math.min(start + CHUNK_SIZE, file.size)

    // 读取当前分片
    const buffer = await readChunk(start, end)

    // 增量计算 MD5
    spark.append(buffer)

    // buffer 会被垃圾回收，内存占用始终保持在 2MB
    currentChunk++

    // 上报进度
    self.postMessage({
      type: 'progress',
      data: { progress: Math.round((currentChunk / totalChunks) * 100) },
    })
  }

  // 完成计算
  const md5 = spark.end()
  self.postMessage({ type: 'complete', data: { md5 } })
}
```

**关键点：**

1. **分片大小**：2MB（平衡内存和性能）
2. **增量计算**：`spark.append()` 支持多次调用
3. **及时释放**：每个 `buffer` 处理完后会被 GC 回收
4. **内存占用**：始终保持在 2MB，不随文件大小增长

**内存占用对比：**

| 文件大小 | 一次性读取  | 分片读取（2MB） |
| -------- | ----------- | --------------- |
| 100MB    | 100MB       | 2MB             |
| 1GB      | 1GB         | 2MB             |
| 5GB      | 5GB（崩溃） | 2MB             |

---

### 4.3 错误处理与边界情况

#### Q20: 如果用户在上传过程中关闭了浏览器标签页，如何处理？

**问题分析：**

```javascript
// 用户关闭标签页 → 所有 JavaScript 执行停止
// - 正在上传的请求被中断
// - 内存中的状态丢失
// - Worker 被销毁
```

**解决方案：**

```typescript
// 方案 1：beforeunload 事件提示用户
window.addEventListener('beforeunload', (e) => {
  if (this.isUploading) {
    e.preventDefault()
    e.returnValue = '文件正在上传，确定要离开吗？'
    return e.returnValue
  }
})

// 方案 2：保存进度到 LocalStorage
class PersistentUploader {
  private saveProgress(fileInfo: UploadFileInfo) {
    const progress = {
      md5: fileInfo.md5,
      uploadId: fileInfo.uploadId,
      uploadedChunks: fileInfo.uploadedChunks,
      timestamp: Date.now(),
    }
    localStorage.setItem(`upload_${fileInfo.md5}`, JSON.stringify(progress))
  }

  private async uploadChunk(chunk: ChunkInfo) {
    await axios.put(chunk.uploadUrl, blob)

    // 每个分片上传成功后保存进度
    fileInfo.uploadedChunks.push(chunk.index)
    this.saveProgress(fileInfo)
  }

  // 用户重新打开页面时恢复
  async resumeFromLocalStorage(md5: string) {
    const saved = localStorage.getItem(`upload_${md5}`)
    if (!saved) return null

    const progress = JSON.parse(saved)

    // 检查是否过期（如 24 小时）
    if (Date.now() - progress.timestamp > 24 * 3600 * 1000) {
      localStorage.removeItem(`upload_${md5}`)
      return null
    }

    return progress
  }
}

// 方案 3：使用 Service Worker（高级方案）
// Service Worker 在后台运行，即使标签页关闭也能继续上传
self.addEventListener('message', async (event) => {
  if (event.data.type === 'upload') {
    const { chunks, uploadUrls } = event.data

    for (let i = 0; i < chunks.length; i++) {
      await fetch(uploadUrls[i], {
        method: 'PUT',
        body: chunks[i],
      })

      // 通知主线程进度
      self.clients.matchAll().then((clients) => {
        clients.forEach((client) => {
          client.postMessage({ type: 'progress', index: i })
        })
      })
    }
  }
})

// 方案 4：使用 Background Sync API（PWA）
// 即使离线也能在网络恢复后继续上传
navigator.serviceWorker.ready.then((registration) => {
  registration.sync.register('upload-sync')
})

self.addEventListener('sync', (event) => {
  if (event.tag === 'upload-sync') {
    event.waitUntil(resumeUpload())
  }
})
```

**对比分析：**

| 方案              | 优点         | 缺点               | 适用场景 |
| ----------------- | ------------ | ------------------ | -------- |
| beforeunload 提示 | 简单         | 只能提示，无法阻止 | 所有场景 |
| LocalStorage 保存 | 可恢复进度   | 需要用户手动恢复   | 一般场景 |
| Service Worker    | 后台继续上传 | 实现复杂，兼容性   | PWA 应用 |
| Background Sync   | 离线也能上传 | 兼容性差           | 高级 PWA |

---

#### Q21: 如果文件在上传过程中被修改了怎么办？

**问题场景：**

```
1. 用户选择文件 file.txt（内容：A）
2. 计算 MD5：abc123
3. 开始上传分片 0-50
4. 用户修改文件内容（内容：B）
5. 继续上传分片 51-100
6. 服务端合并 → 文件损坏（前半部分是 A，后半部分是 B）
```

**检测方案：**

```typescript
// 方案 1：检查文件修改时间
class FileIntegrityChecker {
  async addFile(file: File) {
    const fileInfo: UploadFileInfo = {
      file,
      originalLastModified: file.lastModified, // 记录初始修改时间
      // ...
    }
    return fileInfo
  }

  async uploadChunk(fileInfo: UploadFileInfo, chunk: ChunkInfo) {
    // 上传前检查文件是否被修改
    if (fileInfo.file.lastModified !== fileInfo.originalLastModified) {
      throw new Error('文件已被修改，请重新上传')
    }

    const blob = fileInfo.file.slice(chunk.start, chunk.end)
    await axios.put(chunk.uploadUrl, blob)
  }
}

// 方案 2：分片级别的 MD5 校验
class ChunkIntegrityChecker {
  async uploadChunk(chunk: ChunkInfo) {
    const blob = this.file.slice(chunk.start, chunk.end)

    // 计算分片 MD5
    const chunkMD5 = await this.calculateChunkMD5(blob)

    // 上传时携带 MD5
    await axios.put(chunk.uploadUrl, blob, {
      headers: {
        'Content-MD5': chunkMD5,
        'X-Chunk-Index': chunk.index,
      },
    })

    // 服务端校验分片 MD5，不匹配则拒绝
  }
}

// 方案 3：使用 File Handle API（Chrome 86+）
class FileHandleUploader {
  private fileHandle: FileSystemFileHandle

  async selectFile() {
    // 获取文件句柄（持久引用）
    ;[this.fileHandle] = await window.showOpenFilePicker()
    const file = await this.fileHandle.getFile()
    return file
  }

  async uploadChunk(chunk: ChunkInfo) {
    // 每次读取时重新获取文件（确保是最新版本）
    const file = await this.fileHandle.getFile()

    // 检查文件大小是否变化
    if (file.size !== this.originalSize) {
      throw new Error('文件大小已变化，请重新上传')
    }

    const blob = file.slice(chunk.start, chunk.end)
    await axios.put(chunk.uploadUrl, blob)
  }
}

// 方案 4：锁定文件（操作系统级别）
// 注意：浏览器无法直接锁定文件，需要提示用户
class FileLockedUploader {
  async startUpload(fileInfo: UploadFileInfo) {
    // 提示用户不要修改文件
    this.showWarning('上传期间请勿修改文件')

    try {
      await this.uploadChunks(fileInfo)
    } catch (error) {
      if (this.isFileModifiedError(error)) {
        // 检测到文件被修改
        const retry = confirm('文件已被修改，是否重新上传？')
        if (retry) {
          return this.startUpload(fileInfo)
        }
      }
      throw error
    }
  }
}
```

**推荐方案：**

```typescript
// 综合方案：修改时间 + 大小检查 + 用户提示
class RobustUploader {
  async uploadChunk(fileInfo: UploadFileInfo, chunk: ChunkInfo) {
    // 1. 检查修改时间
    if (fileInfo.file.lastModified !== fileInfo.originalLastModified) {
      throw new FileModifiedError('文件修改时间已变化')
    }

    // 2. 检查文件大小
    if (fileInfo.file.size !== fileInfo.originalSize) {
      throw new FileModifiedError('文件大小已变化')
    }

    // 3. 读取分片
    const blob = fileInfo.file.slice(chunk.start, chunk.end)

    // 4. 检查分片大小
    if (blob.size !== chunk.size) {
      throw new FileModifiedError('分片大小不匹配')
    }

    // 5. 上传
    await axios.put(chunk.uploadUrl, blob)
  }

  async startUpload(fileInfo: UploadFileInfo) {
    try {
      await this.uploadChunks(fileInfo)
    } catch (error) {
      if (error instanceof FileModifiedError) {
        // 文件被修改，提示用户
        const action = await this.showDialog({
          title: '文件已被修改',
          message: '检测到文件在上传过程中被修改，请选择操作：',
          options: ['重新上传', '取消上传'],
        })

        if (action === '重新上传') {
          // 重新计算 MD5 并上传
          return this.startUpload(fileInfo)
        }
      }
      throw error
    }
  }
}
```

---

## 五、系统设计与扩展

### 5.1 服务端设计

#### Q22: 服务端如何设计分片上传的接口？

**完整 API 设计：**

```typescript
// 1. 检查文件是否存在（秒传）
GET /api/upload/check/:md5
Request: 无
Response: {
  code: 700,  // 700: 存在, 701: 不存在
  data: {
    exists: true,
    url: "https://cdn.example.com/files/abc123.mp4"
  }
}

// 2. 初始化上传
POST /api/upload/init
Request: {
  originalName: "video.mp4",
  md5: "abc123...",
  size: 524288000,        // 文件大小（字节）
  chunkSize: 5242880,     // 分片大小（字节）
  chunkNum: 100,          // 分片数量
  contentType: "video/mp4"
}
Response: {
  code: 700,
  data: {
    uploadId: "upload-001",  // 上传任务 ID
    urlList: [               // 剩余分片的预签名 URL
      "https://s3.amazonaws.com/bucket/upload-001/0?signature=...",
      "https://s3.amazonaws.com/bucket/upload-001/1?signature=...",
      // ...
    ]
  }
}

// 3. 上传分片（直接上传到 OSS，不经过业务服务器）
PUT <预签名 URL>
Request: Binary (分片数据)
Response: 200 OK

// 4. 合并分片
POST /api/upload/merge/:md5
Request: 无（服务端根据 uploadId 查询所有分片）
Response: {
  code: 700,
  data: {
    url: "https://cdn.example.com/files/abc123.mp4",
    size: 524288000,
    duration: 3600  // 视频时长（秒）
  }
}

// 5. 取消上传
DELETE /api/upload/:uploadId
Request: 无
Response: {
  code: 700,
  message: "上传已取消，分片已清理"
}
```

**服务端实现（Node.js + MinIO）：**

```javascript
const express = require('express')
const { Client } = require('minio')
const app = express()

// MinIO 客户端
const minioClient = new Client({
  endPoint: 'minio.example.com',
  port: 9000,
  useSSL: true,
  accessKey: 'ACCESS_KEY',
  secretKey: 'SECRET_KEY',
})

// 1. 检查文件是否存在
app.get('/api/upload/check/:md5', async (req, res) => {
  const { md5 } = req.params

  // 查询数据库
  const file = await db.files.findOne({ md5 })

  if (file) {
    return res.json({
      code: 700,
      data: { exists: true, url: file.url },
    })
  }

  res.json({ code: 701, data: { exists: false } })
})

// 2. 初始化上传
app.post('/api/upload/init', async (req, res) => {
  const { originalName, md5, size, chunkSize, chunkNum, contentType } = req.body

  // 生成 uploadId
  const uploadId = `upload_${Date.now()}_${Math.random().toString(36).slice(2)}`

  // 查询已上传的分片
  const uploadedChunks = await db.chunks.find({ uploadId }).select('index')
  const uploadedIndexes = uploadedChunks.map((c) => c.index)

  // 生成剩余分片的预签名 URL
  const urlList = []
  for (let i = 0; i < chunkNum; i++) {
    if (!uploadedIndexes.includes(i)) {
      const objectName = `${uploadId}/${i}`
      const url = await minioClient.presignedPutObject('uploads', objectName, 3600)
      urlList.push(url)
    }
  }

  // 保存上传任务信息
  await db.uploads.create({
    uploadId,
    md5,
    originalName,
    size,
    chunkSize,
    chunkNum,
    contentType,
    status: 'uploading',
    createdAt: new Date(),
  })

  res.json({
    code: 700,
    data: { uploadId, urlList },
  })
})

// 3. 合并分片
app.post('/api/upload/merge/:md5', async (req, res) => {
  const { md5 } = req.params

  // 查询上传任务
  const upload = await db.uploads.findOne({ md5, status: 'uploading' })
  if (!upload) {
    return res.json({ code: 701, message: '上传任务不存在' })
  }

  // 检查所有分片是否上传完成
  const chunks = await db.chunks.find({ uploadId: upload.uploadId })
  if (chunks.length !== upload.chunkNum) {
    return res.json({ code: 702, message: '分片未上传完成' })
  }

  // 合并分片（MinIO 支持 Compose Object）
  const sourceObjects = chunks
    .sort((a, b) => a.index - b.index)
    .map((c) => ({ name: `${upload.uploadId}/${c.index}` }))

  const finalObjectName = `${md5}${path.extname(upload.originalName)}`
  await minioClient.composeObject('files', finalObjectName, sourceObjects)

  // 生成访问 URL
  const url = await minioClient.presignedGetObject('files', finalObjectName, 7 * 24 * 3600)

  // 保存文件记录
  await db.files.create({
    md5,
    originalName: upload.originalName,
    size: upload.size,
    url,
    contentType: upload.contentType,
    createdAt: new Date(),
  })

  // 更新上传任务状态
  await db.uploads.updateOne({ uploadId: upload.uploadId }, { status: 'completed' })

  // 清理分片（异步）
  setTimeout(() => {
    sourceObjects.forEach((obj) => {
      minioClient.removeObject('uploads', obj.name)
    })
  }, 1000)

  res.json({
    code: 700,
    data: { url, size: upload.size },
  })
})
```

---

#### Q23: 如何保证分片上传的顺序和完整性？

**问题分析：**

```
并发上传导致的问题：
- 分片 0、1、2 同时上传
- 分片 1 先完成，分片 0 后完成
- 服务端如何保证按顺序合并？
```

**解决方案：**

```javascript
// 方案 1：分片索引标记（推荐）
// 每个分片携带索引信息，合并时按索引排序
app.post('/api/upload/merge/:md5', async (req, res) => {
  const chunks = await db.chunks.find({ uploadId }).sort({ index: 1 }) // 按索引排序

  // 检查分片连续性
  for (let i = 0; i < chunks.length; i++) {
    if (chunks[i].index !== i) {
      return res.json({ code: 702, message: `分片 ${i} 缺失` })
    }
  }

  // 按顺序合并
  const sourceObjects = chunks.map((c) => ({ name: `${uploadId}/${c.index}` }))
  await minioClient.composeObject('files', finalObjectName, sourceObjects)
})

// 方案 2：ETag 校验
// 每个分片上传后返回 ETag（MD5），合并时校验
app.post('/api/upload/chunk', async (req, res) => {
  const { uploadId, index, etag } = req.body

  // 保存分片信息
  await db.chunks.create({
    uploadId,
    index,
    etag, // 分片的 MD5
    size: req.body.size,
    uploadedAt: new Date(),
  })
})

app.post('/api/upload/merge/:md5', async (req, res) => {
  const chunks = await db.chunks.find({ uploadId }).sort({ index: 1 })

  // 校验每个分片的 ETag
  for (const chunk of chunks) {
    const objectName = `${uploadId}/${chunk.index}`
    const stat = await minioClient.statObject('uploads', objectName)

    if (stat.etag !== chunk.etag) {
      return res.json({ code: 703, message: `分片 ${chunk.index} 校验失败` })
    }
  }

  // 合并
  await minioClient.composeObject('files', finalObjectName, sourceObjects)
})

// 方案 3：最终文件 MD5 校验
app.post('/api/upload/merge/:md5', async (req, res) => {
  // 合并分片
  await minioClient.composeObject('files', finalObjectName, sourceObjects)

  // 下载合并后的文件，计算 MD5
  const stream = await minioClient.getObject('files', finalObjectName)
  const calculatedMD5 = await calculateMD5FromStream(stream)

  // 对比 MD5
  if (calculatedMD5 !== md5) {
    // MD5 不匹配，删除文件
    await minioClient.removeObject('files', finalObjectName)
    return res.json({ code: 704, message: '文件校验失败，请重新上传' })
  }

  res.json({ code: 700, data: { url } })
})

// 方案 4：分片大小校验
app.post('/api/upload/merge/:md5', async (req, res) => {
  const chunks = await db.chunks.find({ uploadId }).sort({ index: 1 })
  const upload = await db.uploads.findOne({ uploadId })

  // 校验每个分片的大小
  for (let i = 0; i < chunks.length; i++) {
    const expectedSize =
      i === chunks.length - 1
        ? upload.size - i * upload.chunkSize // 最后一个分片
        : upload.chunkSize // 其他分片

    if (chunks[i].size !== expectedSize) {
      return res.json({
        code: 705,
        message: `分片 ${i} 大小不匹配，期望 ${expectedSize}，实际 ${chunks[i].size}`,
      })
    }
  }

  // 合并
  await minioClient.composeObject('files', finalObjectName, sourceObjects)
})
```

**推荐方案：**

```javascript
// 综合方案：索引 + 大小 + ETag + 最终 MD5
class ChunkValidator {
  async validateAndMerge(uploadId, expectedMD5) {
    const upload = await db.uploads.findOne({ uploadId })
    const chunks = await db.chunks.find({ uploadId }).sort({ index: 1 })

    // 1. 检查分片数量
    if (chunks.length !== upload.chunkNum) {
      throw new Error(`分片数量不匹配，期望 ${upload.chunkNum}，实际 ${chunks.length}`)
    }

    // 2. 检查分片连续性和大小
    for (let i = 0; i < chunks.length; i++) {
      if (chunks[i].index !== i) {
        throw new Error(`分片 ${i} 缺失`)
      }

      const expectedSize =
        i === chunks.length - 1 ? upload.size - i * upload.chunkSize : upload.chunkSize

      if (chunks[i].size !== expectedSize) {
        throw new Error(`分片 ${i} 大小不匹配`)
      }
    }

    // 3. 合并分片
    const sourceObjects = chunks.map((c) => ({ name: `${uploadId}/${c.index}` }))
    await minioClient.composeObject('files', finalObjectName, sourceObjects)

    // 4. 校验最终文件 MD5
    const stream = await minioClient.getObject('files', finalObjectName)
    const calculatedMD5 = await this.calculateMD5(stream)

    if (calculatedMD5 !== expectedMD5) {
      await minioClient.removeObject('files', finalObjectName)
      throw new Error('文件 MD5 校验失败')
    }

    return { url: await this.getFileUrl(finalObjectName) }
  }
}
```

---

### 5.2 扩展性设计

#### Q24: 如果要支持多文件并发上传，如何设计？

**挑战：**

```
1. 资源竞争：
   - 多个文件同时计算 MD5 → Worker Pool 资源竞争
   - 多个文件同时上传分片 → 带宽竞争

2. 内存占用：
   - 10 个文件 × 3 并发 × 5MB = 150MB

3. 用户体验：
   - 如何展示多个文件的进度？
   - 如何控制总体上传速度？
```

**设计方案：**

```typescript
// 方案 1：全局并发控制
class MultiFileUploader {
  private globalMaxConcurrent = 6 // 全局最大并发数
  private activeUploads = 0 // 当前活跃的上传数
  private uploadQueue: UploadFileInfo[] = [] // 等待上传的文件队列

  async addFiles(files: File[]) {
    for (const file of files) {
      const fileInfo = await this.createFileInfo(file)
      this.uploadQueue.push(fileInfo)
    }

    this.processQueue()
  }

  private async processQueue() {
    while (this.uploadQueue.length > 0 && this.activeUploads < this.globalMaxConcurrent) {
      const fileInfo = this.uploadQueue.shift()!
      this.activeUploads++

      this.uploadFile(fileInfo).finally(() => {
        this.activeUploads--
        this.processQueue() // 继续处理队列
      })
    }
  }

  private async uploadFile(fileInfo: UploadFileInfo) {
    // 单个文件的上传逻辑
    await this.calculateMD5(fileInfo)
    await this.uploadChunks(fileInfo)
    await this.mergeChunks(fileInfo)
  }
}

// 方案 2：动态资源分配
class AdaptiveMultiFileUploader {
  private totalBandwidth = 10 * 1024 * 1024 // 10MB/s
  private files: UploadFileInfo[] = []

  private allocateBandwidth() {
    const uploadingFiles = this.files.filter((f) => f.status === UploadStatus.UPLOADING)
    const bandwidthPerFile = this.totalBandwidth / uploadingFiles.length

    uploadingFiles.forEach((file) => {
      // 根据分配的带宽调整并发数
      file.maxConcurrent = Math.ceil(bandwidthPerFile / (5 * 1024 * 1024)) // 5MB 分片
    })
  }

  async uploadFile(fileInfo: UploadFileInfo) {
    fileInfo.status = UploadStatus.UPLOADING
    this.allocateBandwidth() // 重新分配带宽

    await this.uploadChunks(fileInfo)

    fileInfo.status = UploadStatus.SUCCESS
    this.allocateBandwidth() // 释放带宽给其他文件
  }
}

// 方案 3：优先级队列
class PriorityMultiFileUploader {
  private queue = new PriorityQueue<UploadFileInfo>()

  async addFile(file: File, priority: number = 0) {
    const fileInfo = await this.createFileInfo(file)
    this.queue.enqueue(fileInfo, priority)
    this.processQueue()
  }

  // 小文件优先上传
  async addFiles(files: File[]) {
    for (const file of files) {
      const priority = 1 / file.size // 文件越小，优先级越高
      await this.addFile(file, priority)
    }
  }
}

// 方案 4：分组上传
class GroupedMultiFileUploader {
  async uploadFiles(files: File[]) {
    // 按文件大小分组
    const smallFiles = files.filter((f) => f.size < 10 * 1024 * 1024) // <10MB
    const mediumFiles = files.filter((f) => f.size < 100 * 1024 * 1024) // <100MB
    const largeFiles = files.filter((f) => f.size >= 100 * 1024 * 1024) // >=100MB

    // 小文件：高并发，快速完成
    await this.uploadGroup(smallFiles, { maxConcurrent: 10, chunkConcurrent: 1 })

    // 中等文件：中等并发
    await this.uploadGroup(mediumFiles, { maxConcurrent: 5, chunkConcurrent: 3 })

    // 大文件：低并发，高分片并发
    await this.uploadGroup(largeFiles, { maxConcurrent: 2, chunkConcurrent: 6 })
  }

  private async uploadGroup(files: File[], config: UploadConfig) {
    const promises = files.map((file) => this.uploadFile(file, config))
    await Promise.all(promises)
  }
}
```

**推荐方案：**

```typescript
// 综合方案：全局并发 + 动态分配 + 优先级
class SmartMultiFileUploader {
  private globalMaxConcurrent = 6
  private activeUploads = 0
  private queue = new PriorityQueue<UploadFileInfo>()

  async addFiles(files: File[]) {
    for (const file of files) {
      const fileInfo = await this.createFileInfo(file)

      // 计算优先级：小文件优先
      const priority = 1 / file.size
      this.queue.enqueue(fileInfo, priority)
    }

    this.processQueue()
  }

  private async processQueue() {
    while (!this.queue.isEmpty() && this.activeUploads < this.globalMaxConcurrent) {
      const fileInfo = this.queue.dequeue()!
      this.activeUploads++

      // 动态分配资源
      const availableConcurrency = Math.floor(
        (this.globalMaxConcurrent - this.activeUploads + 1) / this.activeUploads,
      )
      fileInfo.maxConcurrent = Math.max(availableConcurrency, 1)

      this.uploadFile(fileInfo).finally(() => {
        this.activeUploads--
        this.processQueue()
      })
    }
  }
}
```

---

#### Q25: 如果要支持超大文件（如 10GB+），需要做哪些优化？

**挑战：**

```
1. MD5 计算耗时长：10GB 文件计算 MD5 需要 5-10 分钟
2. 分片数量多：10GB ÷ 5MB = 2000 个分片
3. 上传时间长：即使 10MB/s 带宽，也需要 17 分钟
4. 内存占用：2000 个分片信息占用内存
```

**优化方案：**

```typescript
// 1. 采样 MD5（快速计算）
class SampledMD5Calculator {
  async calculateSampledMD5(file: File): Promise<string> {
    const sampleSize = 10 * 1024 * 1024 // 采样 10MB
    const samples: ArrayBuffer[] = []

    // 采样策略：头部 + 中间 + 尾部
    samples.push(await this.readChunk(file, 0, sampleSize)) // 头部
    samples.push(await this.readChunk(file, file.size / 2, sampleSize)) // 中间
    samples.push(await this.readChunk(file, file.size - sampleSize, sampleSize)) // 尾部

    // 计算采样 MD5
    const spark = new SparkMD5.ArrayBuffer()
    samples.forEach((buffer) => spark.append(buffer))

    // 加入文件大小，避免不同文件采样相同
    spark.append(new TextEncoder().encode(file.size.toString()))

    return spark.end()
  }

  // 优势：10GB 文件只需计算 30MB，耗时从 10 分钟降到 1 秒
  // 缺点：不是真正的文件 MD5，只能用于去重，不能用于完整性校验
}

// 2. 增大分片大小
const config: UploadConfig = {
  chunkSize: 10 * 1024 * 1024, // 10MB（而不是 5MB）
  // 10GB ÷ 10MB = 1000 个分片（减少一半）
}

// 3. 分片信息分页加载
class PaginatedChunkManager {
  private chunkPageSize = 100 // 每页 100 个分片
  private currentPage = 0

  async loadNextPage(fileInfo: UploadFileInfo): Promise<ChunkInfo[]> {
    const start = this.currentPage * this.chunkPageSize
    const end = start + this.chunkPageSize

    const chunks = fileInfo.chunks.slice(start, end)
    this.currentPage++

    return chunks
  }

  async uploadChunks(fileInfo: UploadFileInfo) {
    while (this.currentPage * this.chunkPageSize < fileInfo.chunks.length) {
      const chunks = await this.loadNextPage(fileInfo)
      await this.uploadChunkBatch(chunks)
    }
  }
}

// 4. 流式上传（避免一次性读取大分片）
class StreamUploader {
  async uploadChunk(chunk: ChunkInfo) {
    const chunkSize = 1024 * 1024 // 1MB 子分片
    const totalSubChunks = Math.ceil(chunk.size / chunkSize)

    for (let i = 0; i < totalSubChunks; i++) {
      const start = chunk.start + i * chunkSize
      const end = Math.min(start + chunkSize, chunk.end)
      const blob = this.file.slice(start, end)

      // 流式上传（不等待响应）
      await axios.put(chunk.uploadUrl, blob, {
        headers: {
          'Content-Range': `bytes ${start}-${end - 1}/${chunk.size}`,
        },
      })
    }
  }
}

// 5. 后台上传（Service Worker）
class BackgroundUploader {
  async uploadLargeFile(file: File) {
    // 注册 Service Worker
    const registration = await navigator.serviceWorker.register('/upload-worker.js')

    // 将文件信息传递给 Service Worker
    registration.active?.postMessage({
      type: 'upload',
      fileId: file.name,
      fileSize: file.size,
      chunks: this.createChunks(file),
    })

    // 用户可以关闭页面，Service Worker 继续上传
  }
}

// 6. 断点续传优化（服务端记录）
class ServerSideResumeUploader {
  async initUpload(fileInfo: UploadFileInfo) {
    // 服务端记录上传进度
    const response = await axios.post('/api/upload/init', {
      md5: fileInfo.md5,
      size: fileInfo.size,
      chunkSize: this.config.chunkSize,
      chunkNum: fileInfo.chunks.length,
    })

    // 服务端返回已上传的分片索引
    const { uploadId, uploadedChunks } = response.data

    // 只上传剩余分片
    const pendingChunks = fileInfo.chunks.filter((chunk) => !uploadedChunks.includes(chunk.index))

    console.log(`已上传 ${uploadedChunks.length}/${fileInfo.chunks.length} 个分片`)
  }
}

// 7. 多线程并行上传（Web Worker）
class ParallelUploader {
  private uploadWorkers: Worker[] = []

  async uploadChunks(fileInfo: UploadFileInfo) {
    const workerCount = 4
    const chunksPerWorker = Math.ceil(fileInfo.chunks.length / workerCount)

    // 创建多个 Worker，每个负责一部分分片
    for (let i = 0; i < workerCount; i++) {
      const worker = new Worker('./upload.worker.js')
      const chunks = fileInfo.chunks.slice(i * chunksPerWorker, (i + 1) * chunksPerWorker)

      worker.postMessage({
        type: 'upload',
        chunks,
        uploadUrls: chunks.map((c) => c.uploadUrl),
      })

      this.uploadWorkers.push(worker)
    }

    // 等待所有 Worker 完成
    await Promise.all(this.uploadWorkers.map((w) => this.waitForWorker(w)))
  }
}
```

**综合方案：**

```typescript
class LargeFileUploader {
  async uploadLargeFile(file: File) {
    // 1. 采样 MD5（快速去重）
    const sampledMD5 = await this.calculateSampledMD5(file)
    const checkResult = await this.checkFileExists(sampledMD5)

    if (checkResult.exists) {
      // 秒传成功
      return checkResult.url
    }

    // 2. 增大分片（10MB）
    const chunks = this.createChunks(file, 10 * 1024 * 1024)

    // 3. 初始化上传（获取已上传分片）
    const { uploadId, uploadedChunks } = await this.initUpload(file, sampledMD5)

    // 4. 分页上传分片
    const pendingChunks = chunks.filter((c) => !uploadedChunks.includes(c.index))
    const pageSize = 100

    for (let i = 0; i < pendingChunks.length; i += pageSize) {
      const page = pendingChunks.slice(i, i + pageSize)
      await this.uploadChunkBatch(page)

      // 保存进度到服务端
      await this.saveProgress(
        uploadId,
        page.map((c) => c.index),
      )
    }

    // 5. 合并分片
    const result = await this.mergeChunks(uploadId)
    return result.url
  }
}
```

**效果对比：**

| 优化项   | 10GB 文件优化前 | 10GB 文件优化后      |
| -------- | --------------- | -------------------- |
| MD5 计算 | 10 分钟         | 1 秒（采样）         |
| 分片数量 | 2000 个         | 1000 个              |
| 内存占用 | 200MB           | 50MB（分页）         |
| 断点续传 | 前端存储        | 服务端存储（更可靠） |

---

## 六、代码实现细节

### 6.1 关键代码解析

#### Q26: 解释一下 Promise.race 在并发控制中的作用

**代码片段：**

```typescript
const executing: Promise<void>[] = []

for (const chunk of pendingChunks) {
  const promise = this.uploadChunk(fileInfo, chunk).then(() => {
    executing.splice(executing.indexOf(promise), 1)
  })

  executing.push(promise)

  if (executing.length >= this.config.maxConcurrent) {
    await Promise.race(executing) // 关键代码
  }
}

await Promise.all(executing)
```

**详细解析：**

```javascript
// Promise.race 的特性
Promise.race([p1, p2, p3])
// 返回最先完成（resolve 或 reject）的 Promise
// 其他 Promise 仍然继续执行，不会被取消

// 示例：
const p1 = delay(1000).then(() => 'A')
const p2 = delay(2000).then(() => 'B')
const p3 = delay(3000).then(() => 'C')

const result = await Promise.race([p1, p2, p3])
console.log(result) // 1 秒后输出 'A'
// 此时 p2、p3 仍在执行

// 在并发控制中的应用：
// 1. executing 数组维护正在执行的 Promise
// 2. 当数组长度达到 maxConcurrent（如 3）时，等待任意一个完成
// 3. Promise.race 返回后，executing 数组长度变为 2
// 4. 可以继续添加新的 Promise
// 5. 循环往复，保持并发数始终 ≤ maxConcurrent
```

**执行流程可视化：**

```
时间轴：
0s    1s    2s    3s    4s    5s    6s
|-----|-----|-----|-----|-----|-----|

C0 ████████ (2s)
C1 ████████████ (3s)
C2 ██████ (1.5s)
                C3 ████████ (2s)
                     C4 ██████ (1.5s)
                          C5 ████████████ (3s)

说明：
- 0s: 启动 C0, C1, C2（并发数 3）
- 1.5s: C2 完成，启动 C3（并发数仍为 3）
- 2s: C0 完成，启动 C4
- 3s: C1 完成，启动 C5
- 4.5s: C4 完成
- 5s: C3 完成
- 6s: C5 完成

全程并发数 ≤ 3
```

---

#### Q27: Worker Pool 中的任务调度是如何实现的？

**核心代码：**

```typescript
class WorkerPool {
  private workers: Worker[] = []
  private taskQueue: Task<unknown>[] = []
  private activeWorkers = new Map<Worker, Task<unknown>>()

  execute<T>(id: string, data: unknown, onProgress?: (progress: number) => void): Promise<T> {
    return new Promise((resolve, reject) => {
      const task: Task<unknown> = { id, data, resolve, reject, onProgress }
      this.taskQueue.push(task)
      this.processQueue()
    })
  }

  private processQueue() {
    if (this.taskQueue.length === 0) return

    const availableWorker = this.workers.find((w) => !this.activeWorkers.has(w))
    if (!availableWorker) return

    const task = this.taskQueue.shift()!
    this.activeWorkers.set(availableWorker, task)

    availableWorker.postMessage({
      type: 'calculate',
      fileId: task.id,
      file: task.data,
    })
  }

  private releaseWorker(worker: Worker) {
    this.activeWorkers.delete(worker)
    this.processQueue()
  }
}
```

**详细解析：**

```javascript
// 1. 数据结构设计
workers: Worker[]                    // 所有 Worker 实例（线程池）
taskQueue: Task[]                    // 等待执行的任务队列（FIFO）
activeWorkers: Map<Worker, Task>     // Worker → 任务映射（正在执行）

// 2. 任务提交流程
execute(id, data, onProgress) {
  // 步骤 1：将任务封装成 Promise
  return new Promise((resolve, reject) => {
    const task = { id, data, resolve, reject, onProgress };

    // 步骤 2：加入队列
    this.taskQueue.push(task);

    // 步骤 3：尝试执行
    this.processQueue();
  });
}

// 3. 队列处理逻辑
processQueue() {
  // 检查 1：队列是否为空
  if (this.taskQueue.length === 0) return;

  // 检查 2：是否有空闲 Worker
  const availableWorker = this.workers.find(w => !this.activeWorkers.has(w));
  if (!availableWorker) return; // 所有 Worker 都在忙

  // 步骤 1：取出队列头部任务
  const task = this.taskQueue.shift()!;

  // 步骤 2：标记 Worker 为活跃状态
  this.activeWorkers.set(availableWorker, task);

  // 步骤 3：发送任务到 Worker
  availableWorker.postMessage({ type: 'calculate', fileId: task.id, file: task.data });
}

// 4. Worker 完成任务
worker.onmessage = (e) => {
  const task = this.activeWorkers.get(worker);

  if (e.data.type === 'complete') {
    task.resolve(e.data.data);  // 完成 Promise
    this.releaseWorker(worker);  // 释放 Worker
  }
};

// 5. 释放 Worker
releaseWorker(worker) {
  // 步骤 1：从活跃列表中移除
  this.activeWorkers.delete(worker);

  // 步骤 2：处理下一个任务
  this.processQueue();
}
```

**状态转换图：**

```
Worker 状态：
┌─────────┐  processQueue  ┌─────────┐  onmessage  ┌─────────┐
│  空闲   │ ────────────> │  活跃   │ ──────────> │  空闲   │
│ (Idle)  │               │ (Active) │             │ (Idle)  │
└─────────┘               └─────────┘             └─────────┘
     ↑                                                  │
     └──────────────────────────────────────────────────┘
                    releaseWorker

任务状态：
┌─────────┐  push  ┌─────────┐  shift  ┌─────────┐  resolve  ┌─────────┐
│  创建   │ ─────> │  队列   │ ──────> │  执行   │ ────────> │  完成   │
│ (New)   │        │ (Queue) │         │ (Run)   │           │ (Done)  │
└─────────┘        └─────────┘         └─────────┘           └─────────┘
```

**关键设计点：**

1. **FIFO 队列**：保证任务按提交顺序执行
2. **Map 映射**：快速查找 Worker 对应的任务
3. **自动调度**：Worker 完成后自动触发 `processQueue`
4. **Promise 封装**：任务结果通过 Promise 异步返回

---

#### Q28: 如何实现分片上传的进度计算？

**代码实现：**

```typescript
private updateTotalProgress(fileInfo: UploadFileInfo) {
  // 1. 计算总进度（加权平均）
  const totalProgress = fileInfo.chunks.reduce((acc, chunk) => {
    return acc + chunk.progress * chunk.size;
  }, 0);

  fileInfo.progress = Math.round(totalProgress / fileInfo.size);

  // 2. 计算上传速度
  const elapsed = (Date.now() - (fileInfo.startTime || Date.now())) / 1000; // 秒
  if (elapsed > 0) {
    const uploaded = fileInfo.size * (fileInfo.progress / 100);
    fileInfo.speed = uploaded / elapsed; // bytes/s

    // 3. 计算剩余时间
    const remaining = fileInfo.size - uploaded;
    fileInfo.remainingTime = fileInfo.speed > 0 ? remaining / fileInfo.speed : 0;
  }

  // 4. 触发进度回调
  this.events.onProgress?.(fileInfo);
}
```

**详细解析：**

```javascript
// 1. 为什么用加权平均？
// 错误做法：简单平均
const avgProgress = chunks.reduce((acc, c) => acc + c.progress, 0) / chunks.length;
// 问题：不同分片大小不同，最后一个分片可能只有 1MB

// 正确做法：加权平均
const totalProgress = chunks.reduce((acc, chunk) => {
  return acc + chunk.progress * chunk.size; // 进度 × 分片大小
}, 0);
const fileProgress = totalProgress / file.size;

// 示例：
// 文件大小：11MB
// 分片 1：5MB，进度 100% → 贡献 5MB
// 分片 2：5MB，进度 50%  → 贡献 2.5MB
// 分片 3：1MB，进度 0%   → 贡献 0MB
// 总进度：(5 + 2.5 + 0) / 11 = 68%

// 2. 上传速度计算
const elapsed = (Date.now() - startTime) / 1000;  // 已用时间（秒）
const uploaded = fileSize * (progress / 100);     // 已上传字节数
const speed = uploaded / elapsed;                 // bytes/s

// 示例：
// 文件大小：100MB
// 已上传：50%（50MB）
// 已用时间：10 秒
// 速度：50MB / 10s = 5MB/s

// 3. 剩余时间计算
const remaining = fileSize - uploaded;            // 剩余字节数
const remainingTime = remaining / speed;          // 秒

// 示例：
// 剩余：50MB
// 速度：5MB/s
// 剩余时间：50MB / 5MB/s = 10 秒

// 4. 格式化显示
function formatSpeed(bytesPerSecond: number): string {
  if (bytesPerSecond < 1024) return `${bytesPerSecond.toFixed(2)} B/s`;
  if (bytesPerSecond < 1024 * 1024) return `${(bytesPerSecond / 1024).toFixed(2)} KB/s`;
  return `${(bytesPerSecond / 1024 / 1024).toFixed(2)} MB/s`;
}

function formatTime(seconds: number): string {
  if (seconds < 60) return `${Math.round(seconds)} 秒`;
  if (seconds < 3600) return `${Math.round(seconds / 60)} 分钟`;
  return `${Math.round(seconds / 3600)} 小时`;
}
```

**进度更新时机：**

```typescript
// 1. 分片上传进度更新
await axios.put(chunk.uploadUrl, blob, {
  onUploadProgress: (event) => {
    if (event.total) {
      chunk.progress = Math.round((event.loaded / event.total) * 100)
      this.updateTotalProgress(fileInfo) // 每次分片进度变化时更新
    }
  },
})

// 2. 节流优化（避免频繁更新）
class ThrottledProgressUpdater {
  private lastUpdateTime = 0
  private updateInterval = 100 // 100ms 更新一次

  updateProgress(fileInfo: UploadFileInfo) {
    const now = Date.now()
    if (now - this.lastUpdateTime < this.updateInterval) {
      return // 跳过更新
    }

    this.lastUpdateTime = now
    this.calculateProgress(fileInfo)
    this.events.onProgress?.(fileInfo)
  }
}
```

---

## 七、面试表达技巧

### 7.1 如何介绍项目（3 分钟版本）

**开场（30 秒）：**

> 这个项目是一个大文件上传解决方案，主要解决用户上传大文件时遇到的传输不稳定、性能瓶颈等问题。我在简历上写的亮点是：采用分片上传、断点续传、Web Worker 多线程计算、并发控制和重试机制，大幅缩短处理时延，提升用户体验和上传成功率。

**核心流程（1 分钟）：**

> 整个上传流程分为几个关键步骤：
>
> 1. 首先用 Worker Pool 多线程计算文件 MD5，不阻塞主线程
> 2. 然后调用检查接口，支持秒传（相同文件直接返回 URL）
> 3. 如果需要上传，初始化接口会返回预签名 URL 和已上传分片信息，支持断点续传
> 4. 接着并发上传分片，用 Promise.race 控制并发数为 3，充分利用带宽
> 5. 单个分片失败会自动重试，采用指数退避策略
> 6. 所有分片上传完成后，调用合并接口，服务端按顺序合并为完整文件

**技术亮点（1 分钟）：**

> 项目中有几个技术亮点：
>
> 1. Worker Pool 管理多个 Worker 实例，避免线程过多，支持任务队列和自动调度
> 2. 并发控制用 Promise.race 实现，维护一个 executing 数组，保持并发数始终不超过 3
> 3. 断点续传通过服务端记录已上传分片，前端只上传剩余部分
> 4. 内存优化采用分片读取，单个分片只占用 5MB 内存，不随文件大小增长
>
> 实测数据：500MB 文件上传耗时从 215 秒降到 101 秒，失败率从 35% 降到 5%。

**收尾（30 秒）：**

> 这个项目让我深入理解了前端性能优化、并发控制、错误处理等核心技术，也积累了大文件处理的实战经验。如果面试官感兴趣，我可以详细展开任何一个技术点。

---

### 7.2 常见追问及应对策略

**追问 1："你说用了 Web Worker，为什么不直接在主线程计算？"**

> 回答思路：
>
> 1. 先说问题：主线程计算 MD5 会阻塞 UI，大文件可能需要 30-60 秒，期间页面完全卡死
> 2. 再说方案：Worker 在后台线程计算，不影响主线程，用户可以继续操作
> 3. 最后说数据：实测 1GB 文件，主线程耗时 35.6 秒且 UI 卡顿，Worker 耗时 36.1 秒但 UI 流畅
> 4. 进阶：提到 Worker Pool 的设计，避免频繁创建/销毁 Worker

**追问 2："并发控制为什么用 Promise.race 而不是其他方案？"**

> 回答思路：
>
> 1. 先解释 Promise.race 的特性：返回最先完成的 Promise，其他仍继续执行
> 2. 再说应用场景：维护 executing 数组，当长度达到 maxConcurrent 时等待任意一个完成
> 3. 对比其他方案：
>    - Promise.all：需要等所有完成，无法控制并发
>    - async-pool 库：原理类似，但我们自己实现更灵活
> 4. 提到可优化点：用 Map 管理 Promise，支持优先级队列

**追问 3："断点续传如果用户刷新页面怎么办？"**

> 回答思路：
>
> 1. 先承认问题：当前实现数据存储在内存中，刷新会丢失
> 2. 再说解决方案：
>    - LocalStorage：简单场景，保存 MD5、uploadId、uploadedChunks
>    - IndexedDB：大量数据，保存完整分片信息
>    - 服务端存储：最可靠，跨设备、跨浏览器
> 3. 说明选择：项目中采用服务端存储，初始化时返回已上传分片
> 4. 进阶：提到过期清理、数据校验等细节

**追问 4："如果要支持 10GB 超大文件，你会怎么优化？"**

> 回答思路：
>
> 1. 先分析问题：MD5 计算慢、分片数量多、上传时间长、内存占用大
> 2. 再说优化方案：
>    - 采样 MD5：只计算头部、中间、尾部，耗时从 10 分钟降到 1 秒
>    - 增大分片：10MB 而不是 5MB，减少分片数量
>    - 分页加载：分片信息分页加载，减少内存占用
>    - 服务端记录：断点续传信息存储在服务端，更可靠
> 3. 说明权衡：采样 MD5 不能用于完整性校验，只能用于去重
> 4. 进阶：提到流式上传、Service Worker 后台上传等高级方案

**追问 5："你的项目有什么不足或可优化的地方？"**

> 回答思路（展示思考深度）：
>
> 1. 并发控制：
>    - 当前用 indexOf 查找 Promise，可能失败
>    - 优化：用 Map 管理，支持优先级队列
> 2. 错误处理：
>    - 当前单个分片失败会终止整体上传
>    - 优化：用 Promise.allSettled，失败分片单独重试
> 3. 内存管理：
>    - 当前没有监控内存使用
>    - 优化：根据内存占用动态调整并发数
> 4. 用户体验：
>    - 当前没有预估上传时间
>    - 优化：根据网络速度动态调整分片大小和并发数

---

### 7.3 STAR 法则应用

**Situation（背景）：**

> 在实际业务中，用户需要上传大文件（如视频、压缩包），普通上传存在网络不稳定导致失败、无法断点续传、页面卡顿等问题，用户体验很差。

**Task（任务）：**

> 我的任务是设计并实现一个大文件上传解决方案，要求支持分片上传、断点续传、秒传，同时保证页面不卡顿，上传成功率高。

**Action（行动）：**

> 我采用了以下技术方案：
>
> 1. 分片上传：将大文件切分为 5MB 的分片，支持并发上传和失败重试
> 2. Web Worker 多线程：用 Worker Pool 管理多个 Worker 实例，后台计算 MD5，不阻塞主线程
> 3. 并发控制：用 Promise.race 实现，保持并发数为 3，充分利用带宽
> 4. 断点续传：服务端记录已上传分片，前端只上传剩余部分
> 5. 重试机制：单个分片失败自动重试 3 次，采用指数退避策略
> 6. 秒传：计算文件 MD5，服务端检查是否已存在，存在则直接返回 URL

**Result（结果）：**

> 实测数据：
>
> - 500MB 文件上传耗时从 215 秒降到 101 秒，提升 53%
> - 上传失败率从 35% 降到 5%，提升 86%
> - UI 不卡顿，用户可以继续操作
> - 支持暂停/恢复，网络中断后可以继续上传

---

### 7.4 面试中的加分项

**1. 主动提及性能数据：**

> "实测 500MB 文件，优化前耗时 215 秒，优化后 101 秒，提升 53%"
>
> 而不是："优化后速度更快了"

**2. 对比多种方案：**

> "并发控制我对比了 Promise.all、Promise.race、async-pool 库，最终选择 Promise.race 因为..."
>
> 而不是："我用了 Promise.race"

**3. 承认不足并提出优化：**

> "当前实现用 indexOf 查找 Promise 可能失败，更好的方案是用 Map 管理"
>
> 而不是："我的实现很完美"

**4. 展示工程思维：**

> "考虑到用户可能刷新页面，我设计了 LocalStorage + 服务端双重存储"
>
> 而不是："我实现了断点续传"

**5. 引用业界实践：**

> "分片大小选择 5MB 参考了阿里云 OSS 和七牛云的推荐值"
>
> 而不是："我随便选了 5MB"

---

## 八、总结

### 核心技术点回顾

1. **Web Worker 多线程**
   - Worker Pool 管理线程
   - 任务队列和自动调度
   - 分片读取避免内存溢出

2. **并发控制**
   - Promise.race 实现
   - 动态调整并发数
   - 优先级队列优化

3. **断点续传**
   - 服务端记录已上传分片
   - 前端只上传剩余部分
   - 持久化存储（LocalStorage/IndexedDB）

4. **重试机制**
   - 指数退避策略
   - 根据错误类型调整
   - 失败分片单独重试

5. **秒传机制**
   - MD5 唯一标识文件
   - 服务端检查是否存在
   - 权限校验防止滥用

6. **性能优化**
   - 分片读取控制内存
   - 动态调整分片大小
   - 预加载下一批分片

### 面试准备建议

1. **熟悉代码细节**：能够手写核心代码（Worker Pool、并发控制）
2. **准备性能数据**：优化前后的对比数据
3. **思考扩展方案**：超大文件、多文件并发、跨设备续传
4. **了解业界实践**：阿里云 OSS、七牛云、AWS S3 的方案
5. **准备画图**：流程图、状态图、时序图

### 推荐阅读

- [MDN - Web Workers API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API)
- [MDN - File API](https://developer.mozilla.org/en-US/docs/Web/API/File)
- [阿里云 OSS 分片上传](https://help.aliyun.com/document_detail/31850.html)
- [AWS S3 Multipart Upload](https://docs.aws.amazon.com/AmazonS3/latest/userguide/mpuoverview.html)

---

**祝面试顺利！🎉**
